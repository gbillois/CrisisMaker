// Generated from NotPetyaTimeline/CrisisDebrifier.html at commit d11f77e2ce07bbbff7857eb211c7e82a8d161d0e.
// Keep this source isolated in an iframe so its full editor and renderer remain identical.
const CRISIS_DEBRIEF_EDITOR_SOURCE = "<!DOCTYPE html>\n<html lang=\"fr\">\n<head>\n  <meta charset=\"UTF-8\">\n  <title>Crisis Debriefer — Timeline Generator</title>\n  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">\n  <link rel=\"preconnect\" href=\"https://fonts.googleapis.com\">\n  <link rel=\"preconnect\" href=\"https://fonts.gstatic.com\" crossorigin>\n  <link href=\"https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap\" rel=\"stylesheet\">\n  <style>\n    :root {\n      --bg: #edf6f8;\n      --surface: rgba(255, 255, 255, 0.94);\n      --surface-alt: #e6f4f2;\n      --surface-dark: #0b2f4a;\n      --text: #123047;\n      --muted: #5d7384;\n      --border: rgba(11, 82, 109, 0.16);\n      --primary: #0f6d8f;\n      --primary-strong: #0b2f4a;\n      --accent: #30c38f;\n      --accent-soft: #def7ed;\n      --danger: #d9485f;\n      --shadow: 0 24px 60px rgba(11, 47, 74, 0.14);\n      --shadow-sm: 0 10px 28px rgba(11, 47, 74, 0.10);\n      --radius: 18px;\n      --radius-sm: 12px;\n      --ui-font: Inter, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n      --mono-font: \"JetBrains Mono\", ui-monospace, SFMono-Regular, Menlo, monospace;\n    }\n    * { box-sizing: border-box; }\n    html, body { height: 100%; margin: 0; padding: 0; }\n    body {\n      font-family: var(--ui-font);\n      background:\n        radial-gradient(circle at top right, rgba(48, 195, 143, 0.18), transparent 24%),\n        radial-gradient(circle at top left, rgba(15, 109, 143, 0.18), transparent 30%),\n        linear-gradient(180deg, #f8fcfd 0%, var(--bg) 240px);\n      color: var(--text);\n      -webkit-font-smoothing: antialiased;\n      overflow: hidden;\n    }\n    button, input, select, textarea { font: inherit; }\n    button { cursor: pointer; }\n    .app-shell {\n      display: flex;\n      flex-direction: column;\n      height: 100vh;\n    }\n    .nav-topbar {\n      min-height: 72px;\n      background: linear-gradient(120deg, #0b2f4a 0%, #0f6d8f 58%, #30c38f 140%);\n      display: flex;\n      align-items: center;\n      gap: 16px;\n      padding: 0 20px;\n      color: #fff;\n      box-shadow: 0 14px 32px rgba(11, 47, 74, 0.24);\n      position: relative;\n      z-index: 5;\n      flex-shrink: 0;\n    }\n    .brand-mark {\n      width: 42px;\n      height: 42px;\n      border-radius: 13px;\n      background: rgba(255,255,255,0.14);\n      border: 1px solid rgba(255,255,255,0.18);\n      display: grid;\n      place-items: center;\n      font-weight: 700;\n      font-size: 17px;\n      letter-spacing: -0.08em;\n      box-shadow: inset 0 1px 0 rgba(255,255,255,0.14);\n    }\n    .nav-brand-block { display: grid; gap: 2px; min-width: 0; }\n    .nav-brand-eyebrow {\n      color: rgba(255,255,255,0.74);\n      font-size: 0.67rem;\n      font-weight: 700;\n      letter-spacing: 0.16em;\n      text-transform: uppercase;\n    }\n    .nav-project-name {\n      color: rgba(255,255,255,0.98);\n      font-size: 1rem;\n      font-weight: 700;\n    }\n    .nav-context {\n      margin-left: auto;\n      display: flex;\n      align-items: center;\n      gap: 9px;\n      padding: 8px 12px;\n      border-radius: 999px;\n      background: rgba(255,255,255,0.12);\n      border: 1px solid rgba(255,255,255,0.14);\n      color: rgba(255,255,255,0.88);\n      font-size: 0.78rem;\n      font-weight: 600;\n    }\n    .nav-context-dot {\n      width: 8px;\n      height: 8px;\n      border-radius: 50%;\n      background: var(--accent);\n      box-shadow: 0 0 0 4px rgba(48,195,143,0.18);\n    }\n    .editor-workspace {\n      display: grid;\n      grid-template-columns: 390px minmax(0, 1fr);\n      flex: 1;\n      min-height: 0;\n      padding: 18px;\n      gap: 18px;\n    }\n    .panel {\n      background: rgba(255,255,255,0.72);\n      border: 1px solid rgba(15,109,143,0.10);\n      border-radius: var(--radius);\n      box-shadow: var(--shadow);\n      backdrop-filter: blur(16px);\n      overflow-y: auto;\n      padding: 18px 18px 48px;\n    }\n    .preview {\n      display: flex;\n      flex-direction: column;\n      min-width: 0;\n      min-height: 0;\n      overflow: hidden;\n      background: var(--surface);\n      border: 1px solid rgba(15,109,143,0.10);\n      border-radius: var(--radius);\n      box-shadow: var(--shadow);\n    }\n    .preview-toolbar {\n      min-height: 54px;\n      padding: 0 16px;\n      display: flex;\n      align-items: center;\n      gap: 10px;\n      border-bottom: 1px solid var(--border);\n      background: linear-gradient(135deg, rgba(255,255,255,0.96), rgba(230,244,242,0.90));\n      color: var(--primary-strong);\n      font-size: 0.86rem;\n      font-weight: 700;\n      flex-shrink: 0;\n    }\n    .preview-toolbar .live-pill {\n      margin-left: auto;\n      display: inline-flex;\n      align-items: center;\n      gap: 7px;\n      padding: 6px 10px;\n      border-radius: 999px;\n      background: var(--accent-soft);\n      color: #1a8a63;\n      font-size: 0.72rem;\n      letter-spacing: 0.04em;\n      text-transform: uppercase;\n    }\n    .preview-toolbar .live-pill::before {\n      content: \"\";\n      width: 7px;\n      height: 7px;\n      border-radius: 50%;\n      background: var(--accent);\n    }\n    .preview-stage {\n      flex: 1;\n      min-height: 0;\n      padding: 10px;\n      background: linear-gradient(180deg, #f1fafc, #fbfffd 32%, #eef8f4);\n    }\n    .preview iframe {\n      width: 100%;\n      height: 100%;\n      border: 0;\n      display: block;\n      border-radius: 12px;\n      background: #0d0b08;\n      box-shadow: 0 14px 34px rgba(11,47,74,0.16);\n    }\n    .panel-intro {\n      padding: 4px 4px 18px;\n    }\n    h1 { margin: 0; color: var(--primary-strong); font-size: 1.35rem; letter-spacing: -0.025em; }\n    .sub { margin-top: 5px; color: var(--muted); font-size: 0.82rem; line-height: 1.45; }\n    .group {\n      margin-bottom: 14px;\n      padding: 15px;\n      border-radius: var(--radius-sm);\n      border: 1px solid rgba(15,109,143,0.09);\n      background: var(--surface);\n      box-shadow: var(--shadow-sm);\n    }\n    .group > .label {\n      font-size: 0.72rem;\n      font-weight: 700;\n      letter-spacing: 0.10em;\n      text-transform: uppercase;\n      color: var(--muted);\n      margin-bottom: 12px;\n      padding-bottom: 10px;\n      border-bottom: 1px solid var(--border);\n      display: flex;\n      align-items: center;\n      gap: 8px;\n    }\n    .group > .label .dot {\n      width: 9px;\n      height: 9px;\n      border-radius: 50%;\n      background: var(--accent);\n      box-shadow: 0 0 0 4px var(--accent-soft);\n    }\n    button.act {\n      width: 100%;\n      text-align: left;\n      padding: 10px 12px;\n      margin-bottom: 8px;\n      background: var(--surface-alt);\n      border: 1px solid var(--border);\n      color: var(--primary-strong);\n      font-size: 0.79rem;\n      font-weight: 600;\n      border-radius: 10px;\n      transition: transform 0.15s ease, background 0.15s ease, box-shadow 0.15s ease;\n      display: flex;\n      align-items: center;\n      gap: 9px;\n    }\n    button.act:last-of-type { margin-bottom: 0; }\n    button.act:hover { transform: translateY(-1px); background: #dcefeb; box-shadow: 0 8px 18px rgba(11,47,74,0.10); }\n    button.act.primary {\n      background: linear-gradient(135deg, var(--primary) 0%, #18a7a0 100%);\n      color: #fff;\n      border-color: transparent;\n      font-weight: 600;\n      box-shadow: 0 12px 26px rgba(15,109,143,0.24);\n    }\n    button.act.primary:hover { background: linear-gradient(135deg, #0b5f7e 0%, #14958f 100%); }\n    button.act:disabled { opacity: 0.55; cursor: wait; transform: none; }\n    button.act .ico {\n      width: 24px;\n      height: 24px;\n      display: grid;\n      place-items: center;\n      border-radius: 7px;\n      background: rgba(15,109,143,0.10);\n      font-size: 13px;\n      opacity: 0.9;\n      flex-shrink: 0;\n    }\n    button.act.primary .ico { background: rgba(255,255,255,0.16); }\n    button.act .hint { margin-left: auto; font-family: var(--mono-font); font-size: 0.58rem; letter-spacing: 0.08em; opacity: 0.55; text-transform: uppercase; }\n    .toggle-row, .color-row, .text-row {\n      display: flex;\n      align-items: center;\n      gap: 10px;\n      padding: 7px 0;\n      font-size: 0.79rem;\n      color: var(--text);\n    }\n    .toggle-row label, .color-row label, .text-row label { flex: 1; cursor: pointer; }\n    .text-row { flex-direction: column; align-items: stretch; gap: 7px; }\n    .text-row label { flex: none; font-size: 0.75rem; color: var(--primary-strong); font-weight: 600; }\n    input[type=\"text\"], select {\n      width: 100%;\n      min-height: 40px;\n      padding: 9px 11px;\n      background: #fff;\n      border: 1px solid var(--border);\n      color: var(--text);\n      font-size: 0.78rem;\n      border-radius: 10px;\n      transition: border-color 0.15s ease, box-shadow 0.15s ease;\n    }\n    input[type=\"text\"]:focus, select:focus, textarea:focus {\n      outline: none;\n      border-color: rgba(15,109,143,0.48);\n      box-shadow: 0 0 0 4px rgba(15,109,143,0.12);\n    }\n    input[type=\"color\"] {\n      width: 36px; height: 30px; padding: 3px;\n      background: #fff; border: 1px solid var(--border);\n      border-radius: 9px;\n      cursor: pointer;\n    }\n    input[type=\"range\"] { flex: 1; accent-color: var(--primary); cursor: pointer; }\n    .range-row { display: flex; align-items: center; gap: 10px; padding: 6px 0; }\n    .range-row .swatch-label, .color-row .swatch-label { font-family: var(--mono-font); font-size: 0.63rem; color: var(--muted); width: 64px; }\n    .range-row .val { font-family: var(--mono-font); font-size: 0.66rem; color: var(--primary); width: 34px; text-align: right; font-weight: 600; }\n    input[type=\"checkbox\"] { width: 17px; height: 17px; accent-color: var(--primary); cursor: pointer; }\n    textarea {\n      width: 100%;\n      height: 200px;\n      resize: vertical;\n      padding: 11px;\n      background: #f7fbfc;\n      border: 1px solid var(--border);\n      color: var(--primary-strong);\n      font-family: var(--mono-font);\n      font-size: 0.66rem;\n      line-height: 1.5;\n      border-radius: 10px;\n      white-space: pre;\n      overflow: auto;\n    }\n    .err {\n      font-family: var(--mono-font);\n      font-size: 0.65rem;\n      color: var(--danger);\n      margin-top: 6px;\n      min-height: 14px;\n      white-space: pre-wrap;\n    }\n    .note {\n      font-size: 0.71rem;\n      line-height: 1.5;\n      color: var(--muted);\n      background: var(--accent-soft);\n      border: 1px solid rgba(48,195,143,0.20);\n      padding: 10px 11px;\n      border-radius: 10px;\n    }\n    .note code { font-family: var(--mono-font); color: var(--primary); font-size: 0.67rem; font-weight: 600; }\n    input[type=\"file\"] { display: none; }\n    /* ── Content editor (phases & events) ── */\n    .ce-sub { font-size: 0.67rem; font-weight: 700; letter-spacing: 0.10em; text-transform: uppercase; color: var(--muted); margin: 4px 0 9px; }\n    .ce-sub:not(:first-child) { margin-top: 18px; }\n    .ce-actions { display: flex; gap: 8px; margin-top: 4px; }\n    .ce-actions .act { margin-bottom: 0; }\n    .ce-card { border: 1px solid var(--border); border-radius: 10px; background: #fff; margin-bottom: 8px; overflow: hidden; box-shadow: 0 4px 12px rgba(11,47,74,0.05); }\n    .ce-head { display: flex; align-items: center; gap: 8px; padding: 9px 10px; cursor: pointer; background: var(--surface-alt); user-select: none; }\n    .ce-head:hover { background: #dcefeb; }\n    .ce-caret { font-size: 0.62rem; color: var(--muted); transition: transform 0.15s ease; flex-shrink: 0; width: 10px; }\n    .ce-card.open .ce-caret { transform: rotate(90deg); }\n    .ce-chip { width: 9px; height: 9px; border-radius: 50%; flex-shrink: 0; box-shadow: 0 0 0 3px rgba(15,109,143,0.08); }\n    .ce-htitle { flex: 1; min-width: 0; font-size: 0.76rem; font-weight: 600; color: var(--primary-strong); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }\n    .ce-htime { font-family: var(--mono-font); font-size: 0.58rem; color: var(--muted); flex-shrink: 0; }\n    .ce-mini { width: 25px; height: 25px; padding: 0; display: grid; place-items: center; border: 1px solid var(--border); background: #fff; border-radius: 7px; font-size: 0.78rem; line-height: 1; color: var(--primary-strong); flex-shrink: 0; transition: background 0.12s ease, color 0.12s ease, border-color 0.12s ease; }\n    .ce-mini:hover { background: #dcefeb; }\n    .ce-mini:disabled { opacity: 0.3; cursor: default; background: #fff; }\n    .ce-mini.danger:hover { background: #fdecef; color: var(--danger); border-color: rgba(217,72,95,0.45); }\n    .ce-body { display: none; padding: 12px 11px 13px; border-top: 1px solid var(--border); }\n    .ce-card.open .ce-body { display: block; }\n    .ce-field { margin-bottom: 10px; }\n    .ce-field:last-child { margin-bottom: 0; }\n    .ce-field > label { display: block; font-size: 0.65rem; font-weight: 600; color: var(--muted); margin-bottom: 4px; letter-spacing: 0.02em; }\n    .ce-row2 { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; }\n    .ce-row3 { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 8px; }\n    .ce-field input[type=\"number\"] { width: 100%; min-height: 40px; padding: 9px 11px; background: #fff; border: 1px solid var(--border); color: var(--text); font-size: 0.78rem; border-radius: 10px; transition: border-color 0.15s ease, box-shadow 0.15s ease; }\n    .ce-field input[type=\"number\"]:focus { outline: none; border-color: rgba(15,109,143,0.48); box-shadow: 0 0 0 4px rgba(15,109,143,0.12); }\n    .ce-field textarea { height: auto; min-height: 56px; font-family: var(--ui-font); font-size: 0.76rem; line-height: 1.45; white-space: pre-wrap; color: var(--text); background: #fff; }\n    .ce-field input[type=\"color\"] { width: 100%; height: 38px; }\n    #offline-opts { border-color: var(--border) !important; }\n    ::-webkit-scrollbar { width: 9px; height: 9px; }\n    ::-webkit-scrollbar-track { background: transparent; }\n    ::-webkit-scrollbar-thumb { background: rgba(15,109,143,0.20); border-radius: 999px; }\n    ::-webkit-scrollbar-thumb:hover { background: rgba(15,109,143,0.34); }\n    @media (max-width: 900px) {\n      body { overflow: auto; }\n      .app-shell { height: auto; min-height: 100vh; }\n      .nav-context { display: none; }\n      .editor-workspace { grid-template-columns: 1fr; }\n      .panel { max-height: 72vh; }\n      .preview { min-height: 72vh; }\n    }\n    @media (max-width: 520px) {\n      .nav-topbar { padding: 0 12px; gap: 10px; }\n      .brand-mark { width: 38px; height: 38px; border-radius: 11px; }\n      .nav-brand-eyebrow { display: none; }\n      .nav-project-name { font-size: 0.88rem; }\n      .editor-workspace { padding: 10px; gap: 10px; }\n      .panel { padding: 13px 12px 36px; border-radius: 14px; }\n      .group { padding: 13px; }\n      .preview { border-radius: 14px; }\n      .preview-toolbar { padding: 0 12px; }\n      .preview-toolbar .live-pill { font-size: 0; padding: 7px; }\n    }\n  </style>\n</head>\n<body>\n  <div class=\"app-shell\">\n    <header class=\"nav-topbar\">\n      <div class=\"brand-mark\">CD</div>\n      <div class=\"nav-brand-block\">\n        <span class=\"nav-brand-eyebrow\">CrisisMaker by Wavestone</span>\n        <span class=\"nav-project-name\">Crisis Debriefer · Timeline editor</span>\n      </div>\n      <div class=\"nav-context\"><span class=\"nav-context-dot\"></span>Standalone workspace</div>\n    </header>\n\n    <div class=\"editor-workspace\">\n      <aside class=\"panel\">\n        <div class=\"panel-intro\">\n          <h1>Crisis Debriefer</h1>\n          <div class=\"sub\">Configurez, prévisualisez et exportez une timeline de crise interactive.</div>\n        </div>\n\n      <div class=\"group\">\n        <div class=\"label\"><span class=\"dot\"></span>Source</div>\n        <button class=\"act\" id=\"btn-load\"><span class=\"ico\">⇪</span>Charger un fichier de config<span class=\"hint\">.json</span></button>\n        <button class=\"act\" id=\"btn-example\"><span class=\"ico\">◷</span>Exemple StonaWave<span class=\"hint\">CrisisMaker</span></button>\n        <input type=\"file\" id=\"file\" accept=\"application/json,.json\">\n      </div>\n\n      <div class=\"group\">\n        <div class=\"label\"><span class=\"dot\"></span>Export</div>\n        <button class=\"act primary\" id=\"btn-html\"><span class=\"ico\">⤓</span><span id=\"btn-html-txt\">Télécharger la timeline</span><span class=\"hint\">.html</span></button>\n        <button class=\"act\" id=\"btn-json\"><span class=\"ico\">{ }</span>Télécharger la config<span class=\"hint\">.json</span></button>\n        <div class=\"toggle-row\" style=\"margin-top:10px\"><label for=\"x-offline\" style=\"font-weight:600\">100% hors-ligne</label><input type=\"checkbox\" id=\"x-offline\"></div>\n        <div id=\"offline-opts\" style=\"display:none;padding:8px 0 4px 14px;border-left:2px solid rgba(230,220,200,0.1);margin-top:4px\">\n          <div class=\"toggle-row\"><label for=\"x-fonts\">Inclure les polices</label><input type=\"checkbox\" id=\"x-fonts\"></div>\n          <div class=\"toggle-row\"><label for=\"x-mapdata\">Inclure les données cartographiques</label><input type=\"checkbox\" id=\"x-mapdata\" checked></div>\n          <div class=\"note\" style=\"margin-top:8px\">Génération <code>10–30 s</code> selon la connexion.<br>Polices incluses → <code>+250–400 KB</code>.<br>Carte incluse → <code>+400 KB</code>.</div>\n        </div>\n      </div>\n\n      <div class=\"group\">\n        <div class=\"label\"><span class=\"dot\"></span>Blocs affichés</div>\n        <div class=\"toggle-row\"><label for=\"t-map\">Carte / globe</label><input type=\"checkbox\" id=\"t-map\"></div>\n        <div class=\"toggle-row\"><label for=\"t-list\">Liste chronologique</label><input type=\"checkbox\" id=\"t-list\"></div>\n        <div class=\"toggle-row\"><label for=\"t-sev\">Sévérité</label><input type=\"checkbox\" id=\"t-sev\"></div>\n        <div class=\"toggle-row\"><label for=\"t-art\">Artefacts</label><input type=\"checkbox\" id=\"t-art\"></div>\n        <div class=\"toggle-row\"><label for=\"t-play\">Lecture automatique</label><input type=\"checkbox\" id=\"t-play\"></div>\n      </div>\n\n      <div class=\"group\">\n        <div class=\"label\"><span class=\"dot\"></span>Style visuel</div>\n        <div class=\"text-row\"><label for=\"theme-preset\">Style du générateur</label>\n          <select id=\"theme-preset\">\n            <option value=\"cyber-dark\">Cyber dark</option>\n            <option value=\"wavestone\">Wavestone</option>\n          </select>\n        </div>\n        <div class=\"text-row\"><label for=\"m-title\">Titre</label><input type=\"text\" id=\"m-title\"></div>\n        <div class=\"text-row\"><label for=\"m-sub\">Sous-titre</label><input type=\"text\" id=\"m-sub\"></div>\n        <div class=\"text-row\"><label for=\"m-badge\">Badge</label><input type=\"text\" id=\"m-badge\"></div>\n        <div class=\"color-row\"><span class=\"swatch-label\">Fond</span><input type=\"color\" id=\"c-bg\"><label>Arrière-plan</label></div>\n        <div class=\"color-row\"><span class=\"swatch-label\">Texte</span><input type=\"color\" id=\"c-fg\"><label>Corps de texte</label></div>\n        <div class=\"color-row\"><span class=\"swatch-label\">Titres</span><input type=\"color\" id=\"c-ink\"><label>Encre (titres)</label></div>\n        <div class=\"color-row\"><span class=\"swatch-label\">Accent</span><input type=\"color\" id=\"c-accent\"><label>Accent</label></div>\n        <div class=\"color-row\"><span class=\"swatch-label\">Panneaux</span><input type=\"color\" id=\"c-panel\"><label>Bandeaux</label></div>\n        <div class=\"color-row\"><span class=\"swatch-label\">Bordures</span><input type=\"color\" id=\"c-line\"><label>Lignes de séparation</label></div>\n        <div id=\"phase-colors\"></div>\n      </div>\n\n      <div class=\"group\">\n        <div class=\"label\"><span class=\"dot\"></span>Typographie</div>\n        <div class=\"text-row\"><label for=\"f-title\">Police des titres</label><select id=\"f-title\"></select></div>\n        <div class=\"text-row\"><label for=\"f-body\">Police du corps</label><select id=\"f-body\"></select></div>\n        <div class=\"text-row\"><label for=\"f-mono\">Police mono</label><select id=\"f-mono\"></select></div>\n        <div class=\"range-row\"><span class=\"swatch-label\">Échelle</span><input type=\"range\" id=\"s-scale\" min=\"0.8\" max=\"1.3\" step=\"0.05\"><span class=\"val\" id=\"s-scale-val\">1.0</span></div>\n      </div>\n\n      <div class=\"group\">\n        <div class=\"label\"><span class=\"dot\"></span>Disposition</div>\n        <div class=\"text-row\"><label for=\"l-maptype\">Type de carte</label>\n          <select id=\"l-maptype\">\n            <option value=\"globe\">Globe (monde)</option>\n            <option value=\"region\">Région / pays (villes clés)</option>\n            <option value=\"none\">Aucune carte</option>\n          </select>\n        </div>\n        <div class=\"text-row\" id=\"row-bounds\"><label for=\"l-bounds\">Cadrage région — O, S, E, N (vide = auto)</label><input type=\"text\" id=\"l-bounds\" placeholder=\"ex : 22, 44, 41, 53\"></div>\n        <div class=\"text-row\" id=\"row-country\"><label for=\"l-country\">Pays en surbrillance (code ISO numérique, ex : 804)</label><input type=\"text\" id=\"l-country\" placeholder=\"laisser vide = aucun\"></div>\n        <div class=\"text-row\"><label for=\"l-mapside\">Position de la carte</label>\n          <select id=\"l-mapside\"><option value=\"right\">À droite</option><option value=\"left\">À gauche</option></select>\n        </div>\n      </div>\n\n      <div class=\"group\">\n        <div class=\"label\"><span class=\"dot\"></span>Contenu</div>\n        <div class=\"ce-sub\">Phases</div>\n        <div id=\"phase-editor\"></div>\n        <div class=\"ce-actions\"><button class=\"act\" id=\"btn-add-phase\"><span class=\"ico\">＋</span>Ajouter une phase</button></div>\n        <div class=\"ce-sub\">Événements</div>\n        <div id=\"event-editor\"></div>\n        <div class=\"ce-actions\"><button class=\"act\" id=\"btn-add-event\"><span class=\"ico\">＋</span>Ajouter un événement</button></div>\n      </div>\n\n      <div class=\"group\">\n        <div class=\"label\"><span class=\"dot\"></span>Config (JSON)</div>\n        <textarea id=\"json\" spellcheck=\"false\"></textarea>\n        <button class=\"act\" id=\"btn-apply\" style=\"margin-top:8px\"><span class=\"ico\">↻</span>Appliquer le JSON</button>\n        <div class=\"err\" id=\"err\"></div>\n      </div>\n\n      <div class=\"group\">\n        <div class=\"note\">\n          Le fichier d'entrée décrit <code>meta</code>, <code>phases</code> et <code>events</code>.\n          Les sections <code>theme</code> et <code>layout</code> pilotent le style (couleurs, blocs).\n          La position de chaque étape est déduite de <code>t</code> (0→1) ou d'un champ <code>date</code>.\n          L'aperçu est <em>exactement</em> le fichier téléchargé.\n        </div>\n      </div>\n      </aside>\n\n      <main class=\"preview\">\n        <div class=\"preview-toolbar\">\n          Aperçu de la timeline\n          <span class=\"live-pill\">Mise à jour en direct</span>\n        </div>\n        <div class=\"preview-stage\">\n          <iframe id=\"preview\" title=\"Aperçu de la timeline\"></iframe>\n        </div>\n      </main>\n    </div>\n  </div>\n\n  <!-- ════════════════════════════════════════════════════════════════\n       RENDERER SOURCE — non-executing template.\n       This React app reads window.TIMELINE_CONFIG and renders a generic,\n       interactive crisis timeline. It is injected verbatim into every\n       generated standalone file AND into the live preview iframe, so the\n       preview is byte-for-byte identical to the downloaded artifact.\n       ════════════════════════════════════════════════════════════════ -->\n  <script type=\"text/plain\" id=\"tpl-renderer\">\nconst { useState, useEffect, useRef, useMemo, useCallback } = React;\n\nconst CFG    = window.TIMELINE_CONFIG || {};\nconst META   = CFG.meta || {};\nconst THEME  = CFG.theme || {};\nconst LAYOUT = CFG.layout || {};\nconst EVENTS = (CFG.events || []).slice().sort((a, b) => (a.t || 0) - (b.t || 0));\nconst PHASES = CFG.phases || [];\nconst KIND_LABELS = CFG.kindLabels || {};\nconst MAP = CFG.map || {};\nconst STYLE = THEME.preset === 'wavestone' ? 'wavestone' : 'cyber-dark';\nconst isWavestone = STYLE === 'wavestone';\n\n// ── Theme tokens (working style knobs; extend here for future themes) ──\nconst C = {\n  bg:     THEME.bg     || '#0d0b08',\n  fg:     THEME.fg     || '#e6dcc8',\n  ink:    THEME.ink    || '#f6f1e4',\n  accent: THEME.accent || '#dc3c28',\n  panel:  THEME.panel  || '#0a0907',\n  line:   THEME.line   || 'rgba(230,220,200,0.12)',\n  muted:  THEME.muted  || 'rgba(230,220,200,0.55)',\n  subtle: isWavestone ? '#817C95' : 'rgba(230,220,200,0.4)',\n  faint: isWavestone ? '#E6E4EE' : 'rgba(230,220,200,0.12)',\n  surfaceAlt: isWavestone ? '#F5F4F9' : 'rgba(230,220,200,0.05)',\n  hover: isWavestone ? '#F1EEFB' : 'rgba(230,220,200,0.03)',\n  borderStrong: isWavestone ? '#CFCCDC' : 'rgba(230,220,200,0.25)',\n  onAccent: isWavestone ? '#150939' : '#0a0907',\n  mapBg: isWavestone ? '#F5F4F9' : '#0a0907',\n  mapLand: isWavestone ? 'rgba(69,29,199,0.10)' : 'rgba(230,220,200,0.12)',\n  mapLine: isWavestone ? 'rgba(69,29,199,0.30)' : 'rgba(230,220,200,0.28)',\n  radius: isWavestone ? 16 : 0,\n  radiusSm: isWavestone ? 8 : 2,\n  shadow: isWavestone ? '0 12px 28px rgba(22,18,31,0.12)' : 'none',\n  brandGradient: 'radial-gradient(120% 120% at 25% 15%, #5226E0 0%, #451DC7 38%, #2D1380 100%)',\n};\n// ── Typography tokens (configurable font families) ──\nconst F = {\n  title: (THEME.fontTitle || 'Fraunces') + (isWavestone ? \", 'Montserrat', sans-serif\" : \", 'Times New Roman', serif\"),\n  body:  (THEME.fontBody  || 'Inter') + \", sans-serif\",\n  mono:  (THEME.fontMono  || 'JetBrains Mono') + \", monospace\",\n};\n\n// ── Layout blocks (toggle-able presence) ──\nconst showMap      = LAYOUT.showMap       !== false && EVENTS.some(e => Array.isArray(e.coords));\nconst showList     = LAYOUT.showEventList !== false;\nconst showSeverity = LAYOUT.showSeverity  !== false;\nconst showArtifacts= LAYOUT.showArtifacts !== false;\nconst showPlayback = LAYOUT.showPlayback  !== false;\n\nconst DEFAULT_PHASE_COLORS = ['#d4a03c','#dc3c28','#b4afa5','#6c9b7d','#7d8bb0','#b07d9b'];\nconst phaseById = (id) => PHASES.find(p => p.id === id);\nconst phaseColor = (id) => {\n  const p = phaseById(id);\n  if (p && p.color) return p.color;\n  const i = PHASES.findIndex(x => x.id === id);\n  return i >= 0 ? DEFAULT_PHASE_COLORS[i % DEFAULT_PHASE_COLORS.length] : C.accent;\n};\nconst phaseLabel = (id) => {\n  const p = phaseById(id);\n  const base = p ? (p.label || id) : (id || '');\n  return String(base).split('·')[0].trim().toUpperCase();\n};\nconst kindLabel = (k) => k ? (KIND_LABELS[k] || String(k).toUpperCase()) : '';\nconst shortDate = (d) => String(d || '').split(',')[0].toUpperCase();\nconst hexToRgba = (hex, a) => {\n  if (!hex || hex[0] !== '#') return hex;\n  let h = hex.slice(1);\n  if (h.length === 3) h = h.split('').map(c => c + c).join('');\n  const n = parseInt(h, 16);\n  return 'rgba(' + ((n>>16)&255) + ',' + ((n>>8)&255) + ',' + (n&255) + ',' + a + ')';\n};\nconst slug = (META.title || 'timeline').toLowerCase().replace(/[^a-z0-9]+/g, '-');\n\n// ═══════════════════════ SHARED: coastline data ═══════════════════════\n// Fetch world-atlas land once and decode to [lat,lon] rings. Reused by both\n// the globe and the region (single-country) map so adding the second mode\n// costs almost nothing in payload.\nconst useLand = () => {\n  const [land, setLand] = React.useState(null);\n  React.useEffect(() => {\n    let cancelled = false;\n    const process = (topo) => {\n      if (cancelled) return;\n      const t = topo.transform, sx = t.scale[0], sy = t.scale[1], tx = t.translate[0], ty = t.translate[1];\n      const decodeArc = (arc) => { let x=0,y=0; return arc.map(([dx,dy]) => { x+=dx; y+=dy; return [x*sx+tx, y*sy+ty]; }); };\n      const arcs = topo.arcs.map(decodeArc);\n      const lineFromArcIdx = (idx) => { const rev = idx < 0; const arc = arcs[rev ? ~idx : idx]; return rev ? [...arc].reverse() : arc; };\n      const polys = [];\n      const addRing = (arcIdxs) => {\n        const ring = [];\n        arcIdxs.forEach((ai, i) => { const seg = lineFromArcIdx(ai); for (let k = i===0?0:1; k<seg.length; k++) ring.push([seg[k][1], seg[k][0]]); });\n        polys.push(ring);\n      };\n      const walk = (g) => {\n        if (g.type === 'Polygon') g.arcs.forEach(addRing);\n        else if (g.type === 'MultiPolygon') g.arcs.forEach(p => p.forEach(addRing));\n        else if (g.type === 'GeometryCollection') g.geometries.forEach(walk);\n      };\n      walk(topo.objects.land);\n      setLand(polys);\n    };\n    if (window.__LAND_CACHE__) { process(window.__LAND_CACHE__); return; }\n    fetch('https://cdn.jsdelivr.net/npm/world-atlas@2/land-110m.json')\n      .then(r => r.json())\n      .then(topo => { if (!window.__LAND_CACHE__) window.__LAND_CACHE__ = topo; process(topo); })\n      .catch(() => {});\n    return () => { cancelled = true; };\n  }, []);\n  return land;\n};\n\n// ═══════════════════════ SHARED: country outlines ═════════════════════════\n// Fetches countries-110m.json once; returns [{id, rings}] per country polygon.\n// id is the ISO-3166-1 numeric code. Reused by both Globe and RegionMap.\nconst useCountries = () => {\n  const [countries, setCountries] = React.useState(null);\n  React.useEffect(() => {\n    let cancelled = false;\n    const process = (topo) => {\n      if (cancelled) return;\n      const t = topo.transform, sx = t.scale[0], sy = t.scale[1], tx = t.translate[0], ty = t.translate[1];\n      const decodeArc = (arc) => { let x=0,y=0; return arc.map(([dx,dy]) => { x+=dx; y+=dy; return [x*sx+tx, y*sy+ty]; }); };\n      const arcs = topo.arcs.map(decodeArc);\n      const lineFromArcIdx = (idx) => { const rev = idx < 0; const arc = arcs[rev ? ~idx : idx]; return rev ? [...arc].reverse() : arc; };\n      const decodeRing = (arcIdxs) => {\n        const ring = [];\n        arcIdxs.forEach((ai, i) => { const seg = lineFromArcIdx(ai); for (let k = i===0?0:1; k<seg.length; k++) ring.push([seg[k][1], seg[k][0]]); });\n        return ring;\n      };\n      const result = [];\n      const walk = (g) => {\n        if (g.type === 'Polygon') result.push({ id: g.id, rings: g.arcs.map(decodeRing) });\n        else if (g.type === 'MultiPolygon') g.arcs.forEach(poly => result.push({ id: g.id, rings: poly.map(decodeRing) }));\n        else if (g.type === 'GeometryCollection') g.geometries.forEach(walk);\n      };\n      walk(topo.objects.countries);\n      setCountries(result);\n    };\n    if (window.__COUNTRIES_CACHE__) { process(window.__COUNTRIES_CACHE__); return; }\n    fetch('https://cdn.jsdelivr.net/npm/world-atlas@2/countries-110m.json')\n      .then(r => r.json())\n      .then(topo => { if (!window.__COUNTRIES_CACHE__) window.__COUNTRIES_CACHE__ = topo; process(topo); })\n      .catch(() => {});\n    return () => { cancelled = true; };\n  }, []);\n  return countries;\n};\n\n// ═══════════════════════ GLOBE ═══════════════════════\nconst Globe = ({ events, currentTime, activeEvent }) => {\n  const size = 460, cx = size/2, cy = size/2, R = size*0.46;\n  const targetLon = activeEvent && activeEvent.coords ? activeEvent.coords[1] : 20;\n  const targetLat = activeEvent && activeEvent.coords ? activeEvent.coords[0] : 30;\n  const [rot, setRot] = React.useState({ lon: targetLon, lat: targetLat });\n  const rafRef = React.useRef(null);\n  React.useEffect(() => {\n    const tl = targetLon, tla = Math.max(-60, Math.min(60, targetLat * 0.35));\n    const animate = () => {\n      setRot(prev => {\n        let dl = tl - prev.lon;\n        if (dl > 180) dl -= 360; if (dl < -180) dl += 360;\n        const dla = tla - prev.lat;\n        if (Math.abs(dl) < 0.1 && Math.abs(dla) < 0.1) return prev;\n        return { lon: prev.lon + dl*0.06, lat: prev.lat + dla*0.06 };\n      });\n      rafRef.current = requestAnimationFrame(animate);\n    };\n    rafRef.current = requestAnimationFrame(animate);\n    return () => cancelAnimationFrame(rafRef.current);\n  }, [targetLon, targetLat]);\n\n  const project = (lat, lon) => {\n    const f = lat*Math.PI/180, l = (lon - rot.lon)*Math.PI/180, f0 = rot.lat*Math.PI/180;\n    const cosc = Math.sin(f0)*Math.sin(f) + Math.cos(f0)*Math.cos(f)*Math.cos(l);\n    if (cosc < 0) return [0, 0, false];\n    const x = R*Math.cos(f)*Math.sin(l);\n    const y = R*(Math.cos(f0)*Math.sin(f) - Math.sin(f0)*Math.cos(f)*Math.cos(l));\n    return [cx + x, cy - y, true];\n  };\n\n  const landmasses = useLand();\n  const countries  = useCountries();\n\n  const firedEvents = events.filter(e => e.t <= currentTime && e.coords);\n  return (\n    <div style={{ width:'100%', height:'100%', display:'flex', alignItems:'center', justifyContent:'center' }}>\n      <svg viewBox={`0 0 ${size} ${size}`} style={{ width:'100%', height:'100%', maxHeight:'100%' }}>\n        <defs>\n          <radialGradient id=\"oceanGrad\" cx=\"40%\" cy=\"40%\">\n            <stop offset=\"0%\" stopColor={isWavestone ? '#FFFFFF' : '#1a1611'} /><stop offset=\"60%\" stopColor={C.mapBg} /><stop offset=\"100%\" stopColor={isWavestone ? '#E6E4EE' : '#08070a'} />\n          </radialGradient>\n          <radialGradient id=\"rim\" cx=\"50%\" cy=\"50%\">\n            <stop offset=\"85%\" stopColor={hexToRgba(C.ink,0)} /><stop offset=\"95%\" stopColor={hexToRgba(C.ink,0.15)} /><stop offset=\"100%\" stopColor={hexToRgba(C.ink,0)} />\n          </radialGradient>\n          <radialGradient id=\"shade\" cx=\"70%\" cy=\"70%\">\n            <stop offset=\"0%\" stopColor=\"rgba(0,0,0,0)\" /><stop offset=\"100%\" stopColor=\"rgba(0,0,0,0.55)\" />\n          </radialGradient>\n          <radialGradient id=\"pulseGrad\">\n            <stop offset=\"0%\" stopColor={hexToRgba(C.accent, 0.7)} /><stop offset=\"60%\" stopColor={hexToRgba(C.accent, 0.18)} /><stop offset=\"100%\" stopColor={hexToRgba(C.accent, 0)} />\n          </radialGradient>\n        </defs>\n        <circle cx={cx} cy={cy} r={R} fill=\"url(#oceanGrad)\" />\n        <g stroke={hexToRgba(C.ink,0.07)} strokeWidth=\"0.6\" fill=\"none\">\n          {[-60,-30,0,30,60].map(lat => {\n            const pts = [];\n            for (let lon=-180; lon<=180; lon+=6) { const [x,y,v]=project(lat,lon); if (v) pts.push(`${x},${y}`); else if (pts.length) pts.push('M'); }\n            return pts.join(' ').split(' M ').filter(s=>s.trim()).map((seg,i)=> <polyline key={`lat${lat}-${i}`} points={seg} />);\n          })}\n          {[-150,-120,-90,-60,-30,0,30,60,90,120,150,180].map(lon => {\n            const pts = [];\n            for (let lat=-85; lat<=85; lat+=5) { const [x,y,v]=project(lat,lon); if (v) pts.push(`${x},${y}`); else if (pts.length) pts.push('M'); }\n            return pts.join(' ').split(' M ').filter(s=>s.trim()).map((seg,i)=> <polyline key={`lon${lon}-${i}`} points={seg} />);\n          })}\n        </g>\n        {/* land fill */}\n        <g>\n          {(landmasses || []).map((poly, i) => {\n            const runs = []; let cur = [];\n            for (const [lat, lon] of poly) { const [x,y,v]=project(lat,lon); if (v) cur.push(`${x.toFixed(1)},${y.toFixed(1)}`); else if (cur.length) { runs.push(cur); cur=[]; } }\n            if (cur.length) runs.push(cur);\n            return runs.map((r, j) => r.length < 2 ? null : (\n              <polyline key={`${i}-${j}`} points={r.join(' ')} fill={C.mapLand} stroke=\"none\" />\n            ));\n          })}\n        </g>\n        {/* country borders */}\n        <g>\n          {(countries || []).map((country, ci) =>\n            country.rings.map((poly, ri) => {\n              const runs = []; let cur = [];\n              for (const [lat, lon] of poly) { const [x,y,v]=project(lat,lon); if (v) cur.push(`${x.toFixed(1)},${y.toFixed(1)}`); else if (cur.length) { runs.push(cur); cur=[]; } }\n              if (cur.length) runs.push(cur);\n              return runs.map((r, j) => r.length < 2 ? null : (\n                <polyline key={`cb${ci}-${ri}-${j}`} points={r.join(' ')} fill=\"none\" stroke={C.mapLine} strokeWidth=\"0.4\" />\n              ));\n            })\n          )}\n        </g>\n        <circle cx={cx} cy={cy} r={R} fill=\"url(#shade)\" pointerEvents=\"none\" />\n        {firedEvents.map(evt => {\n          const [x, y, visible] = project(evt.coords[0], evt.coords[1]);\n          if (!visible) return null;\n          const age = currentTime - evt.t;\n          const ageScale = Math.max(0, 1 - age*2.5);\n          const baseR = 2 + (evt.severity || 1)*1.1;\n          const pulseR = baseR + ageScale*22;\n          const color = phaseColor(evt.phase);\n          const isActive = activeEvent && evt.id === activeEvent.id;\n          return (\n            <g key={evt.id}>\n              {ageScale > 0.02 && <circle cx={x} cy={y} r={pulseR} fill=\"url(#pulseGrad)\" opacity={ageScale} />}\n              {isActive && (\n                <circle cx={x} cy={y} r={baseR+8} fill=\"none\" stroke={color} strokeWidth=\"1\" opacity=\"0.6\">\n                  <animate attributeName=\"r\" from={baseR+4} to={baseR+18} dur=\"1.4s\" repeatCount=\"indefinite\" />\n                  <animate attributeName=\"opacity\" from=\"0.7\" to=\"0\" dur=\"1.4s\" repeatCount=\"indefinite\" />\n                </circle>\n              )}\n              <circle cx={x} cy={y} r={baseR} fill={color} stroke={C.mapBg} strokeWidth=\"1\" />\n            </g>\n          );\n        })}\n        <circle cx={cx} cy={cy} r={R} fill=\"url(#rim)\" pointerEvents=\"none\" />\n        <circle cx={cx} cy={cy} r={R} fill=\"none\" stroke={hexToRgba(C.ink,0.18)} strokeWidth=\"0.8\" />\n      </svg>\n    </div>\n  );\n};\n\n// ═══════════════════════ REGION MAP (single country / area + cities) ═══════\n// 2D map cropped to a bounding box. If MAP.bounds is omitted it auto-fits to\n// the event coordinates, so \"pick a country\" can be as simple as choosing the\n// region mode when all your key cities live there. Event coords are the cities.\nconst RegionMap = ({ events, currentTime, activeEvent }) => {\n  const land      = useLand();\n  const countries = useCountries();\n  const W = 460, H = 460, PAD = 24;\n  const geo = events.filter(e => Array.isArray(e.coords));\n\n  let b = Array.isArray(MAP.bounds) && MAP.bounds.length === 4 ? MAP.bounds.slice() : null;\n  if (!b) {\n    if (geo.length) {\n      const lats = geo.map(e => e.coords[0]), lons = geo.map(e => e.coords[1]);\n      const padLat = Math.max(2, (Math.max.apply(null, lats) - Math.min.apply(null, lats)) * 0.18);\n      const padLon = Math.max(2, (Math.max.apply(null, lons) - Math.min.apply(null, lons)) * 0.18);\n      b = [Math.min.apply(null, lons) - padLon, Math.min.apply(null, lats) - padLat, Math.max.apply(null, lons) + padLon, Math.max.apply(null, lats) + padLat];\n    } else { b = [-180, -60, 180, 85]; }\n  }\n  const west = b[0], south = b[1], east = b[2], north = b[3];\n  const lonSpan = Math.max(0.0001, east - west), latSpan = Math.max(0.0001, north - south);\n  const s = Math.min((W - PAD*2) / lonSpan, (H - PAD*2) / latSpan);\n  const offX = (W - lonSpan*s) / 2, offY = (H - latSpan*s) / 2;\n  const project = (lat, lon) => [offX + (lon - west)*s, offY + (north - lat)*s];\n  const inBox = (lat, lon) => lon >= west-2 && lon <= east+2 && lat >= south-2 && lat <= north+2;\n\n  const firedEvents = events.filter(e => e.t <= currentTime && e.coords);\n  return (\n    <div style={{ width:'100%', height:'100%', display:'flex', alignItems:'center', justifyContent:'center' }}>\n      <svg viewBox={`0 0 ${W} ${H}`} style={{ width:'100%', height:'100%', maxHeight:'100%' }}>\n        <defs>\n          <radialGradient id=\"rPulse\"><stop offset=\"0%\" stopColor={hexToRgba(C.accent,0.7)} /><stop offset=\"60%\" stopColor={hexToRgba(C.accent,0.18)} /><stop offset=\"100%\" stopColor={hexToRgba(C.accent,0)} /></radialGradient>\n          <clipPath id=\"rClip\"><rect x=\"0\" y=\"0\" width={W} height={H} /></clipPath>\n        </defs>\n        <rect x=\"0\" y=\"0\" width={W} height={H} fill={C.mapBg} />\n        {/* graticule */}\n        <g clipPath=\"url(#rClip)\" stroke={hexToRgba(C.ink,0.06)} strokeWidth=\"0.6\" fill=\"none\">\n          {(() => {\n            const lines = [];\n            const stepLat = niceStep(latSpan), stepLon = niceStep(lonSpan);\n            for (let la = Math.ceil(south/stepLat)*stepLat; la <= north; la += stepLat) { const a = project(la, west), c = project(la, east); lines.push(<line key={`la${la}`} x1={a[0]} y1={a[1]} x2={c[0]} y2={c[1]} />); }\n            for (let lo = Math.ceil(west/stepLon)*stepLon; lo <= east; lo += stepLon) { const a = project(south, lo), c = project(north, lo); lines.push(<line key={`lo${lo}`} x1={a[0]} y1={a[1]} x2={c[0]} y2={c[1]} />); }\n            return lines;\n          })()}\n        </g>\n        {/* land fill */}\n        <g clipPath=\"url(#rClip)\">\n          {(land || []).map((poly, i) => {\n            if (!poly.some(([la, lo]) => inBox(la, lo))) return null;\n            const pts = poly.map(([la, lo]) => { const p = project(la, lo); return `${p[0].toFixed(1)},${p[1].toFixed(1)}`; }).join(' ');\n            return <polyline key={i} points={pts} fill={C.mapLand} stroke=\"none\" />;\n          })}\n        </g>\n        {/* country borders + optional highlight */}\n        <g clipPath=\"url(#rClip)\">\n          {(countries || []).map((country, ci) => {\n            const isHL = MAP.country != null && String(country.id) === String(MAP.country);\n            return country.rings.map((poly, ri) => {\n              if (!poly.some(([la, lo]) => inBox(la, lo))) return null;\n              const pts = poly.map(([la, lo]) => { const p = project(la, lo); return `${p[0].toFixed(1)},${p[1].toFixed(1)}`; }).join(' ');\n              return <polyline key={`c${ci}-${ri}`} points={pts}\n                fill={isHL ? 'rgba(220,180,100,0.18)' : 'none'}\n                stroke={isHL ? hexToRgba(C.accent,0.65) : C.mapLine}\n                strokeWidth={isHL ? '0.9' : '0.5'}\n                strokeLinejoin=\"round\" />;\n            });\n          })}\n        </g>\n        <rect x=\"0\" y=\"0\" width={W} height={H} fill=\"none\" stroke={hexToRgba(C.ink,0.18)} strokeWidth=\"1\" />\n        {/* cities */}\n        <g clipPath=\"url(#rClip)\">\n          {firedEvents.map(evt => {\n            const [x, y] = project(evt.coords[0], evt.coords[1]);\n            const age = currentTime - evt.t, ageScale = Math.max(0, 1 - age*2.5);\n            const baseR = 2.5 + (evt.severity || 1)*1.1, pulseR = baseR + ageScale*20;\n            const color = phaseColor(evt.phase), isActive = activeEvent && evt.id === activeEvent.id;\n            const cityLabel = String(evt.location || '').split(',')[0];\n            return (\n              <g key={evt.id}>\n                {ageScale > 0.02 && <circle cx={x} cy={y} r={pulseR} fill=\"url(#rPulse)\" opacity={ageScale} />}\n                {isActive && (\n                  <circle cx={x} cy={y} r={baseR+8} fill=\"none\" stroke={color} strokeWidth=\"1\" opacity=\"0.6\">\n                    <animate attributeName=\"r\" from={baseR+4} to={baseR+18} dur=\"1.4s\" repeatCount=\"indefinite\" />\n                    <animate attributeName=\"opacity\" from=\"0.7\" to=\"0\" dur=\"1.4s\" repeatCount=\"indefinite\" />\n                  </circle>\n                )}\n                <circle cx={x} cy={y} r={baseR} fill={color} stroke={C.mapBg} strokeWidth=\"1\" />\n                {(isActive || ageScale > 0.25) && cityLabel && (\n                  <text x={x + baseR + 4} y={y + 3} fill={isActive ? C.ink : C.muted} fontFamily={F.mono} fontSize=\"9\" style={{ letterSpacing: '0.04em' }}>{cityLabel}</text>\n                )}\n              </g>\n            );\n          })}\n        </g>\n      </svg>\n    </div>\n  );\n};\nconst niceStep = (span) => { const r = span / 5; const p = Math.pow(10, Math.floor(Math.log10(r))); const m = r / p; return (m < 1.5 ? 1 : m < 3.5 ? 2 : m < 7.5 ? 5 : 10) * p; };\n\n// ═══════════════════════ TIMELINE TRACK ═══════════════════════\nconst TimelineTrack = ({ events, phases, currentTime, setCurrentTime, activeId, setActiveId, setPlaying }) => {\n  const trackRef = React.useRef(null);\n  const [hoverT, setHoverT] = React.useState(null);\n  const [dragging, setDragging] = React.useState(false);\n\n  const tFromEvent = (e) => { const r = trackRef.current.getBoundingClientRect(); return Math.max(0, Math.min(1, (e.clientX - r.left)/r.width)); };\n  const onMove = (e) => { if (!trackRef.current) return; const t = tFromEvent(e); if (dragging) setCurrentTime(t); else setHoverT(t); };\n  const onDown = (e) => { if (!trackRef.current) return; setCurrentTime(tFromEvent(e)); setDragging(true); setPlaying(false); };\n\n  React.useEffect(() => {\n    if (!dragging) return;\n    const up = () => setDragging(false);\n    const move = (e) => { if (trackRef.current) setCurrentTime(tFromEvent(e)); };\n    window.addEventListener('mouseup', up); window.addEventListener('mousemove', move);\n    return () => { window.removeEventListener('mouseup', up); window.removeEventListener('mousemove', move); };\n  }, [dragging]);\n\n  // Auto-sampled ticks across the chronology\n  const ticks = useMemo(() => {\n    if (!events.length) return [];\n    const N = Math.min(9, events.length), out = [], seen = new Set();\n    for (let i = 0; i < N; i++) {\n      const idx = Math.round(i * (events.length - 1) / Math.max(1, N - 1));\n      const e = events[idx];\n      const key = Math.round(e.t * 100);\n      if (seen.has(key)) continue; seen.add(key);\n      out.push({ t: e.t, label: shortDate(e.dateLabel) });\n    }\n    return out;\n  }, [events]);\n\n  const firstC = phaseColor(phases[0] && phases[0].id);\n  const lastC  = phaseColor(phases[phases.length-1] && phases[phases.length-1].id);\n\n  return (\n    <div style={{ width:'100%', position:'relative' }}>\n      {/* Phase labels */}\n      <div style={{ position:'relative', height:18, marginBottom:4 }}>\n        {phases.map(p => (\n          <div key={p.id} style={{ position:'absolute', left:`${p.start*100}%`, width:`${(p.end-p.start)*100}%`, top:0, paddingLeft:8,\n            fontFamily:F.mono, fontSize:9, letterSpacing:'0.14em', textTransform:'uppercase',\n            color: phaseColor(p.id), fontWeight:700, whiteSpace:'nowrap', overflow:'hidden', textOverflow:'ellipsis' }}>\n            <span>{p.label}</span>\n            {p.range && <span style={{ opacity:0.55, marginLeft:8, fontWeight:500 }}>{p.range}</span>}\n          </div>\n        ))}\n      </div>\n      {/* Ticks */}\n      <div style={{ position:'relative', height:16, marginBottom:6, fontFamily:F.mono, fontSize:9.5, color:C.subtle, letterSpacing:'0.08em' }}>\n        {ticks.map((tick, i) => (\n          <div key={i} style={{ position:'absolute', left:`${tick.t*100}%`,\n            transform: tick.t >= 0.999 ? 'translateX(-100%)' : tick.t <= 0.001 ? 'translateX(0)' : 'translateX(-50%)', whiteSpace:'nowrap' }}>\n            <span style={{ display:'inline-block', borderLeft:`1px solid ${C.borderStrong}`, paddingLeft:4, paddingTop:1 }}>{tick.label}</span>\n          </div>\n        ))}\n      </div>\n      {/* Track */}\n      <div ref={trackRef} onMouseMove={onMove} onMouseLeave={() => setHoverT(null)} onMouseDown={onDown}\n        style={{ position:'relative', height:62, cursor:'pointer', userSelect:'none' }}>\n        {phases.map(p => (\n          <div key={p.id} style={{ position:'absolute', left:`${p.start*100}%`, width:`${(p.end-p.start)*100}%`, top:22, height:18,\n            background: hexToRgba(phaseColor(p.id), 0.14), borderLeft:`1px solid ${C.line}` }} />\n        ))}\n        <div style={{ position:'absolute', left:0, width:`${currentTime*100}%`, top:22, height:18,\n          background:`linear-gradient(90deg, ${hexToRgba(firstC,0.35)} 0%, ${hexToRgba(C.accent,0.5)} 50%, ${hexToRgba(lastC,0.25)} 100%)`,\n          pointerEvents:'none', mixBlendMode:isWavestone?'multiply':'screen' }} />\n        <div style={{ position:'absolute', left:0, right:0, top:30, height:2, background:C.borderStrong, pointerEvents:'none' }} />\n        {events.map((evt, i) => {\n          const active = evt.id === activeId, fired = evt.t <= currentTime, color = phaseColor(evt.phase);\n          const stagger = (i % 2 === 0) ? -1 : 1;\n          return (\n            <div key={evt.id} onClick={(e) => { e.stopPropagation(); setActiveId(evt.id); setCurrentTime(evt.t); setPlaying(false); }}\n              style={{ position:'absolute', left:`${evt.t*100}%`, top: stagger>0?42:10, transform:'translateX(-50%)', cursor:'pointer', zIndex: active?20:10 }} title={evt.title}>\n              <div style={{ width: active?12:8, height: active?12:8, background: fired?color:'transparent', border:`1.5px solid ${color}`, borderRadius:'50%',\n                transition:'all 180ms', boxShadow: active?`0 0 0 4px ${color}33, 0 0 12px ${color}`:'none' }} />\n              <div style={{ position:'absolute', left:'50%', top: stagger>0?-12:8, height:14, width:1, background: fired?color:C.faint, transform:'translateX(-50%)' }} />\n            </div>\n          );\n        })}\n        <div style={{ position:'absolute', left:`${currentTime*100}%`, top:0, bottom:0, width:2, transform:'translateX(-50%)', background:C.ink, pointerEvents:'none', boxShadow:`0 0 12px ${hexToRgba(C.ink,0.35)}` }}>\n          <div style={{ position:'absolute', top:-4, left:'50%', transform:'translateX(-50%)', width:10, height:10, background:C.ink, clipPath:'polygon(0 0, 100% 0, 50% 100%)' }} />\n        </div>\n        {hoverT !== null && !dragging && (\n          <div style={{ position:'absolute', left:`${hoverT*100}%`, top:0, bottom:0, width:1, transform:'translateX(-50%)', background:hexToRgba(C.ink,0.3), pointerEvents:'none' }} />\n        )}\n      </div>\n    </div>\n  );\n};\n\n// ═══════════════════════ DETAIL + LIST ═══════════════════════\nconst MetaRow = ({ label, value, highlight }) => (\n  <div style={{ display:'flex', justifyContent:'space-between', alignItems:'baseline', gap:16, padding:'6px 0', borderBottom:`1px dashed ${C.line}` }}>\n    <span style={{ fontFamily:F.mono, fontSize:10, letterSpacing:'0.14em', textTransform:'uppercase', color:C.muted }}>{label}</span>\n    <span style={{ fontFamily:F.mono, fontSize:13, color: highlight || C.ink, fontWeight:600, textAlign:'right' }}>{value}</span>\n  </div>\n);\n\nconst SEV_LABELS = ['','LOW','MODERATE','HIGH','SEVERE','CRITICAL'];\nconst EventDetail = ({ event }) => {\n  if (!event) return (\n    <div style={{ padding:24, color:C.subtle, fontFamily:F.mono, fontSize:12, letterSpacing:'0.04em' }}>\n      Scrub the timeline or click an event to inspect the record.\n    </div>\n  );\n  const color = phaseColor(event.phase);\n  return (\n    <div style={{ padding:'28px 32px 36px', fontFamily:F.body, color: C.fg, position:'relative', overflowY:'auto', height:'100%', boxSizing:'border-box' }}>\n      <div style={{ display:'flex', alignItems:'center', gap:12, marginBottom:20, fontFamily:F.mono, fontSize:10, letterSpacing:'0.16em', textTransform:'uppercase' }}>\n        {event.phase && <span style={{ padding:'3px 8px', background:color, color:C.onAccent, fontWeight:700, borderRadius:C.radiusSm }}>{phaseLabel(event.phase)}</span>}\n        {event.kind && <span style={{ color:C.muted }}>{kindLabel(event.kind)}</span>}\n        <span style={{ color:C.subtle, marginLeft:'auto', fontSize:10 }}>FILE #{String(event.id).toUpperCase()}</span>\n      </div>\n      {(event.dateLabel || event.location) && (\n        <div style={{ fontFamily:F.mono, fontSize:11, color:C.muted, letterSpacing:'0.08em', marginBottom:6 }}>\n          {[event.dateLabel, event.location].filter(Boolean).join(' · ').toUpperCase()}\n        </div>\n      )}\n      <h2 style={{ margin:'0 0 18px 0', fontFamily:F.title, fontSize:34, fontWeight:500, lineHeight:1.08, letterSpacing:'-0.01em', color:C.ink }}>{event.title}</h2>\n      {showSeverity && event.severity && (\n        <div style={{ display:'flex', alignItems:'center', gap:10, marginBottom:20, fontFamily:F.mono, fontSize:10, letterSpacing:'0.1em', color:C.muted }}>\n          <span>SEVERITY</span>\n          <div style={{ display:'flex', gap:3 }}>{[1,2,3,4,5].map(i => <div key={i} style={{ width:14, height:6, background: i<=event.severity?color:C.faint, borderRadius:isWavestone?999:0 }} />)}</div>\n          <span style={{ color:C.subtle, marginLeft:8 }}>{SEV_LABELS[event.severity]}</span>\n        </div>\n      )}\n      {event.headline && (\n        <div style={{ borderLeft:`2px solid ${color}`, paddingLeft:16, marginBottom:22, fontFamily:F.title, fontSize:17, lineHeight:1.45, color:C.fg, fontStyle:'italic' }}>{event.headline}</div>\n      )}\n      {event.body && <div style={{ fontSize:14.5, lineHeight:1.65, color:C.fg, marginBottom:22 }}>{event.body}</div>}\n      {(event.casualties || event.damageUSD || (showArtifacts && event.artifacts && event.artifacts.length)) && (\n        <div style={{ display:'grid', gridTemplateColumns:'1fr', gap:12, marginTop:24, paddingTop:20, borderTop:`1px solid ${C.line}` }}>\n          {event.casualties && <MetaRow label=\"Impact\" value={event.casualties} />}\n          {event.damageUSD && <MetaRow label=\"Damage\" value={event.damageUSD} highlight={color} />}\n          {showArtifacts && event.artifacts && event.artifacts.length > 0 && (\n            <div>\n              <div style={{ fontFamily:F.mono, fontSize:10, letterSpacing:'0.14em', textTransform:'uppercase', color:C.subtle, marginBottom:8 }}>Artifacts</div>\n              <div style={{ display:'flex', flexWrap:'wrap', gap:6 }}>\n                {event.artifacts.map((a, i) => (\n                  <span key={i} style={{ fontFamily:F.mono, fontSize:11, padding:'3px 8px', background:C.surfaceAlt, border:`1px solid ${C.line}`, borderRadius:C.radiusSm, color:C.fg }}>{a}</span>\n                ))}\n              </div>\n            </div>\n          )}\n        </div>\n      )}\n    </div>\n  );\n};\n\nconst EventList = ({ events, activeId, onSelect, currentTime }) => {\n  const listRef = React.useRef(null), activeRef = React.useRef(null);\n  React.useEffect(() => {\n    if (activeRef.current && listRef.current) {\n      const li = activeRef.current, ul = listRef.current;\n      const liTop = li.offsetTop - ul.offsetTop, liBottom = liTop + li.offsetHeight;\n      if (liTop < ul.scrollTop) ul.scrollTop = liTop - 8;\n      else if (liBottom > ul.scrollTop + ul.clientHeight) ul.scrollTop = liBottom - ul.clientHeight + 8;\n    }\n  }, [activeId]);\n  return (\n    <div ref={listRef} style={{ height:'100%', overflowY:'auto', padding:'16px 0' }}>\n      <div style={{ padding:'0 20px 12px', fontFamily:F.mono, fontSize:10, letterSpacing:'0.18em', textTransform:'uppercase', color:C.subtle }}>\n        Chronology · {events.length} records\n      </div>\n      {events.map(evt => {\n        const active = evt.id === activeId, fired = evt.t <= currentTime, color = phaseColor(evt.phase);\n        return (\n          <div key={evt.id} ref={active?activeRef:null} onClick={() => onSelect(evt)}\n            style={{ padding:'10px 20px 10px 16px', borderLeft:`3px solid ${active?color:fired?color+'66':'transparent'}`,\n              background: active?C.surfaceAlt:'transparent', cursor:'pointer', transition:'background 150ms', display:'flex', gap:10, alignItems:'flex-start' }}\n            onMouseEnter={(e)=>{ if(!active) e.currentTarget.style.background=C.hover; }}\n            onMouseLeave={(e)=>{ if(!active) e.currentTarget.style.background='transparent'; }}>\n            <div style={{ fontFamily:F.mono, fontSize:9, letterSpacing:'0.08em', color: fired?C.muted:C.subtle, width:54, flexShrink:0, paddingTop:2, lineHeight:1.4 }}>\n              {shortDate(evt.dateLabel)}\n            </div>\n            <div style={{ flex:1, minWidth:0 }}>\n              <div style={{ fontFamily:F.body, fontSize:12.5, fontWeight: active?600:500, color: active?C.ink:fired?C.fg:C.subtle, lineHeight:1.3 }}>{evt.title}</div>\n              {evt.kind && <div style={{ fontFamily:F.mono, fontSize:9, color: color+'bb', letterSpacing:'0.12em', marginTop:3, textTransform:'uppercase' }}>{kindLabel(evt.kind)}</div>}\n            </div>\n          </div>\n        );\n      })}\n    </div>\n  );\n};\n\n// ═══════════════════════ CHROME ═══════════════════════\nconst Stat = ({ label, value, accent, mono }) => (\n  <div style={{ display:'flex', flexDirection:'column', gap:2, minWidth:0 }}>\n    <span style={{ fontFamily:F.mono, fontSize:9, letterSpacing:'0.14em', textTransform:'uppercase', color:isWavestone?'rgba(255,255,255,0.58)':C.subtle }}>{label}</span>\n    <span style={{ fontFamily: mono!==false?F.mono:F.body, fontSize:13, fontWeight:600, color: accent?C.accent:(isWavestone?'#FFFFFF':C.ink), fontVariantNumeric:'tabular-nums' }}>{value}</span>\n  </div>\n);\nconst LegendDot = ({ color, label }) => (\n  <div style={{ display:'flex', alignItems:'center', gap:6 }}><div style={{ width:8, height:8, borderRadius:'50%', background:color }} /><span>{label}</span></div>\n);\nconst PlayButton = ({ playing, onClick }) => (\n  <button onClick={onClick} style={{ width:40, height:40, background: playing?C.surfaceAlt:C.accent, border:`1px solid ${C.line}`, color: playing?C.ink:C.onAccent, cursor:'pointer', display:'flex', alignItems:'center', justifyContent:'center', borderRadius:isWavestone?999:2, boxShadow:playing?'none':C.shadow }} title={playing?'Pause (space)':'Play (space)'}>\n    {playing ? <svg width=\"12\" height=\"14\" viewBox=\"0 0 14 16\"><rect x=\"1\" y=\"0\" width=\"4\" height=\"16\" fill=\"currentColor\"/><rect x=\"9\" y=\"0\" width=\"4\" height=\"16\" fill=\"currentColor\"/></svg>\n             : <svg width=\"12\" height=\"14\" viewBox=\"0 0 14 16\"><path d=\"M1 0 L13 8 L1 16 Z\" fill=\"currentColor\"/></svg>}\n  </button>\n);\nconst ctrlBtnStyle = { width:34, height:34, background:C.surfaceAlt, border:`1px solid ${C.line}`, color:C.fg, cursor:'pointer', fontSize:14, fontFamily:F.mono, borderRadius:isWavestone?999:2 };\nconst SpeedControl = ({ speed, setSpeed }) => (\n  <div style={{ display:'flex', gap:1, fontFamily:F.mono, fontSize:10 }}>\n    {[1,2,4,8].map(s => (\n      <button key={s} onClick={() => setSpeed(s)} style={{ padding:'8px 10px', background: speed===s?(isWavestone?'#451DC7':C.fg):C.surfaceAlt, color: speed===s?(isWavestone?'#FFFFFF':C.onAccent):C.fg, border:`1px solid ${C.line}`, cursor:'pointer', fontWeight: speed===s?700:500, borderRadius:C.radiusSm }}>{s}×</button>\n    ))}\n  </div>\n);\n\n// ═══════════════════════ APP ═══════════════════════\nconst App = () => {\n  const events = EVENTS, phases = PHASES;\n  const firstId = events[0] ? events[0].id : null;\n  const lsKey = (k) => `cdb:${slug}:${k}`;\n  const [currentTime, setCurrentTimeRaw] = useState(() => { try { const v = parseFloat(localStorage.getItem(lsKey('t')) || '0'); return isFinite(v)?Math.max(0,Math.min(1,v)):0; } catch { return 0; } });\n  const [activeId, setActiveIdRaw] = useState(() => { try { return localStorage.getItem(lsKey('active')) || firstId; } catch { return firstId; } });\n  const [playing, setPlaying] = useState(false);\n  const [speed, setSpeed] = useState(1);\n\n  const setCurrentTime = useCallback((t) => { setCurrentTimeRaw(t); try { localStorage.setItem(lsKey('t'), String(t)); } catch {} }, []);\n  const setActiveId = useCallback((id) => { setActiveIdRaw(id); try { localStorage.setItem(lsKey('active'), id); } catch {} }, []);\n\n  useEffect(() => {\n    const upcoming = events.filter(e => e.t <= currentTime + 0.001);\n    if (upcoming.length) { const latest = upcoming[upcoming.length-1]; if (latest.id !== activeId) setActiveId(latest.id); }\n  }, [currentTime]);\n\n  useEffect(() => {\n    if (!playing) return;\n    let raf, last = null;\n    const step = (ts) => {\n      if (last == null) last = ts;\n      const dt = (ts - last)/1000; last = ts;\n      setCurrentTimeRaw(prev => {\n        const next = prev + (dt/60)*speed;\n        if (next >= 1) { setPlaying(false); try { localStorage.setItem(lsKey('t'), '1'); } catch {} return 1; }\n        try { localStorage.setItem(lsKey('t'), String(next)); } catch {}\n        return next;\n      });\n      raf = requestAnimationFrame(step);\n    };\n    raf = requestAnimationFrame(step);\n    return () => cancelAnimationFrame(raf);\n  }, [playing, speed]);\n\n  useEffect(() => {\n    const onKey = (e) => {\n      if (e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA') return;\n      if (e.code === 'Space' && showPlayback) { e.preventDefault(); setPlaying(p => !p); }\n      else if (e.code === 'ArrowLeft') { e.preventDefault(); const idx = events.findIndex(x=>x.id===activeId); const n = events[Math.max(0,idx-1)]; if (n){ setActiveId(n.id); setCurrentTime(n.t); setPlaying(false); } }\n      else if (e.code === 'ArrowRight') { e.preventDefault(); const idx = events.findIndex(x=>x.id===activeId); const n = events[Math.min(events.length-1,idx+1)]; if (n){ setActiveId(n.id); setCurrentTime(n.t); setPlaying(false); } }\n      else if (e.key === '0' || e.code === 'Home') { setCurrentTime(0); if (firstId) setActiveId(firstId); setPlaying(false); }\n    };\n    window.addEventListener('keydown', onKey);\n    return () => window.removeEventListener('keydown', onKey);\n  }, [activeId, events]);\n\n  const active = events.find(e => e.id === activeId);\n  const firedCount = events.filter(e => e.t <= currentTime).length;\n  const [vw, setVw] = useState(() => typeof window !== 'undefined' ? window.innerWidth : 1400);\n  useEffect(() => { const onR = () => setVw(window.innerWidth); window.addEventListener('resize', onR); return () => window.removeEventListener('resize', onR); }, []);\n  const compact = vw < 1400;\n  const leftW = compact ? 170 : 280, rightW = compact ? 250 : 460;\n  const currentPhase = phases.find(p => currentTime >= p.start && currentTime < p.end) || phases[phases.length-1] || {};\n\n  // Adaptive column layout: present blocks + map side (alternate layouts)\n  const mapSide = LAYOUT.mapSide === 'left' ? 'left' : 'right';\n  const order = (mapSide === 'left' ? ['map','detail','list'] : ['list','detail','map'])\n    .filter(k => k === 'detail' || (k === 'list' && showList) || (k === 'map' && showMap));\n  const colOf = {}; order.forEach((k, i) => { colOf[k] = i + 1; });\n  const widthOf = { list: `${leftW}px`, detail: '1fr', map: `${rightW}px` };\n  const gridCols = order.map(k => widthOf[k]).join(' ');\n  const listCol = colOf.list, detailCol = colOf.detail, mapCol = colOf.map;\n  const sep = `1px solid ${C.line}`;\n  const edge = (selfCol, otherCol) => otherCol == null ? {} : (otherCol > selfCol ? { borderRight: sep } : { borderLeft: sep });\n\n  return (\n    <div style={{ height:'100%', width:'100%', display:'grid', gridTemplateRows:'auto 36vh 1fr', gridTemplateColumns: gridCols, background:C.bg, color:C.fg, fontFamily:F.body, overflow:'hidden', gap:isWavestone?12:0, padding:isWavestone?12:0 }}>\n      {/* Header */}\n      <header style={{ gridColumn:'1 / -1', borderBottom:`1px solid ${C.line}`, padding:'14px 28px 12px', display:'flex', alignItems:'center', gap:20, background:isWavestone?C.brandGradient:C.panel, borderRadius:C.radius, boxShadow:C.shadow, flexWrap:'wrap' }}>\n        <div style={{ display:'flex', alignItems:'center', gap:10 }}>\n          <div style={{ width:10, height:10, background:C.accent, borderRadius:isWavestone?'50%':1, animation:'pulse 1.6s infinite' }} />\n          {META.badge && <div style={{ fontFamily:F.mono, fontSize:10, letterSpacing:'0.2em', color:isWavestone?'rgba(255,255,255,0.72)':C.muted, textTransform:'uppercase' }}>{META.badge}</div>}\n        </div>\n        <div style={{ fontFamily:F.title, fontSize:22, fontWeight:isWavestone?700:500, letterSpacing:'-0.01em', color:isWavestone?'#FFFFFF':C.ink }}>\n          {META.title || 'Timeline'}{META.subtitle && <span style={{ color:isWavestone?'rgba(255,255,255,0.64)':C.subtle, fontStyle:isWavestone?'normal':'italic', fontWeight:400 }}> {META.subtitle}</span>}\n        </div>\n        <div style={{ marginLeft:'auto', display:'flex', gap:24, alignItems:'center', flexWrap:'wrap' }}>\n          <Stat label=\"Records\" value={`${firedCount} / ${events.length}`} />\n          {currentPhase.label && <Stat label=\"Phase\" value={phaseLabel(currentPhase.id)} mono={false} accent />}\n          <Stat label=\"Playhead\" value={`${Math.round(currentTime*100)}%`} />\n        </div>\n      </header>\n\n      {/* Timeline hero */}\n      <section style={{ gridColumn:'1 / -1', gridRow:'2 / 3', background:isWavestone?C.panel:`linear-gradient(180deg, ${C.panel} 0%, ${C.bg} 100%)`, border:`1px solid ${C.line}`, borderRadius:C.radius, boxShadow:C.shadow, padding:'22px 40px 24px', display:'flex', flexDirection:'column', gap:14, overflow:'hidden' }}>\n        <div style={{ display:'flex', alignItems:'center', gap:16, flexWrap:'wrap' }}>\n          {currentPhase.label && (\n            <span style={{ padding:'4px 12px', background: phaseColor(currentPhase.id), color:C.onAccent, fontFamily:F.mono, fontWeight:700, letterSpacing:'0.18em', textTransform:'uppercase', fontSize:10, borderRadius:isWavestone?999:0 }}>\n              ▸ PHASE · {phaseLabel(currentPhase.id)}\n            </span>\n          )}\n          {currentPhase.range && <span style={{ fontFamily:F.mono, fontSize:11, letterSpacing:'0.1em', color:C.muted }}>{currentPhase.range}</span>}\n          {active && <span style={{ marginLeft:10, fontFamily:F.title, fontSize:15, fontStyle:isWavestone?'normal':'italic', color:C.fg }}>\n            <span style={{ color:C.subtle }}>viewing · </span>{active.dateLabel}</span>}\n          {showPlayback && (\n            <div style={{ marginLeft:'auto', display:'flex', gap:10, alignItems:'center' }}>\n              <PlayButton playing={playing} onClick={() => setPlaying(p => !p)} />\n              <button onClick={() => { setCurrentTime(0); if (firstId) setActiveId(firstId); setPlaying(false); }} style={ctrlBtnStyle} title=\"Reset\">⏮</button>\n              {vw >= 1050 && <SpeedControl speed={speed} setSpeed={setSpeed} />}\n            </div>\n          )}\n        </div>\n        <div style={{ flex:1, minHeight:0, display:'flex', alignItems:'center' }}>\n          <div style={{ width:'100%' }}>\n            <TimelineTrack events={events} phases={phases} currentTime={currentTime} setCurrentTime={setCurrentTime} activeId={activeId} setActiveId={setActiveId} setPlaying={setPlaying} />\n          </div>\n        </div>\n        <div style={{ fontFamily:F.mono, fontSize:9.5, letterSpacing:'0.14em', color:C.subtle, textTransform:'uppercase', textAlign:'center' }}>\n          ← → step events &nbsp;·&nbsp; {showPlayback ? 'space play/pause · ' : ''}drag track to scrub &nbsp;·&nbsp; click pins to jump\n        </div>\n      </section>\n\n      {/* Row 3 */}\n      {showList && (\n        <aside style={{ gridRow:'3 / 4', gridColumn: String(listCol), ...edge(listCol, detailCol), overflow:'hidden', background:isWavestone?C.panel:'rgba(10,9,7,0.6)', borderRadius:C.radius, boxShadow:C.shadow, minHeight:0 }}>\n          <EventList events={events} activeId={activeId} onSelect={(e) => { setActiveId(e.id); setCurrentTime(e.t); setPlaying(false); }} currentTime={currentTime} />\n        </aside>\n      )}\n      <main style={{ gridRow:'3 / 4', gridColumn: String(detailCol), overflow:'hidden', background:isWavestone?C.panel:'rgba(10,9,7,0.4)', borderRadius:C.radius, boxShadow:C.shadow, minHeight:0, ...(mapSide==='right' ? edge(detailCol, mapCol) : edge(detailCol, listCol)) }}>\n        <EventDetail event={active} />\n      </main>\n      {showMap && (\n        <section style={{ gridRow:'3 / 4', gridColumn: String(mapCol), ...edge(mapCol, detailCol), padding:'20px 24px 22px', display:'flex', flexDirection:'column', gap:12, overflow:'hidden', minWidth:0, minHeight:0, borderRadius:C.radius, boxShadow:C.shadow, background:isWavestone?C.panel:'radial-gradient(ellipse at center, rgba(20,18,15,0.8) 0%, rgba(10,9,7,1) 70%)' }}>\n          <div style={{ display:'flex', alignItems:'baseline', justifyContent:'space-between', fontFamily:F.mono, fontSize:9.5, letterSpacing:'0.18em', textTransform:'uppercase', color:C.muted }}>\n            <span>{MAP.label || (MAP.mode === 'region' ? 'Incident Map' : 'Incident Globe')}</span>{active && active.location && <span style={{ color:C.subtle }}>{String(active.location).split(',')[0]}</span>}\n          </div>\n          <div style={{ flex:1, minHeight:0, display:'flex', alignItems:'center', justifyContent:'center' }}>\n            {MAP.mode === 'region'\n              ? <RegionMap events={events} currentTime={currentTime} activeEvent={active} />\n              : <Globe events={events} currentTime={currentTime} activeEvent={active} />}\n          </div>\n          <div style={{ display:'flex', gap:16, justifyContent:'center', flexWrap:'wrap', fontFamily:F.mono, fontSize:9.5, letterSpacing:'0.14em', textTransform:'uppercase', color:C.muted }}>\n            {phases.map(p => <LegendDot key={p.id} color={phaseColor(p.id)} label={phaseLabel(p.id)} />)}\n          </div>\n        </section>\n      )}\n    </div>\n  );\n};\n\nReactDOM.createRoot(document.getElementById('root')).render(<App />);\n  </script>\n\n  <!-- ════════════════════════════════════════════════════════════════\n       SHELL LOGIC — config management, live preview, file I/O, export.\n       ════════════════════════════════════════════════════════════════ -->\n  <script>\n  (function () {\n    'use strict';\n\n    // ── Built-in example: the NotPetya reconstruction ──\n    const DEFAULT_CONFIG = {\n      meta: {\n        title: 'NotPetya',\n        subtitle: '— an incident reconstruction',\n        badge: 'CLASSIFIED · DOSSIER 2017-27/06',\n        lang: 'en',\n      },\n      theme: { preset: 'cyber-dark', bg: '#0d0b08', fg: '#e6dcc8', accent: '#dc3c28', panel: '#0a0907' },\n      layout: { showMap: true, showEventList: true, showSeverity: true, showArtifacts: true, showPlayback: true },\n      phases: [\n        { id: 'prelude',    label: 'Pre-crisis · Weaponization',       range: '2013 — May 2017',     start: 0.00, end: 0.40, color: '#d4a03c' },\n        { id: 'detonation', label: 'The Crisis',                       range: 'Jun 27, 2017 — Day 0', start: 0.40, end: 0.68, color: '#dc3c28' },\n        { id: 'fallout',    label: 'Post-crisis · Years-long fallout', range: 'Jul 2017 — 2023',      start: 0.68, end: 1.00, color: '#b4afa5' },\n      ],\n      events: [\n        { id: 'e01', phase: 'prelude', dateLabel: 'c. 2013', t: 0.00, title: `EternalBlue is developed inside the NSA`, location: `Fort Meade, Maryland`, coords: [39.1,-76.7], severity: 1, kind: 'intelligence', headline: `A zero-day SMBv1 exploit is weaponized inside the NSA's Tailored Access Operations unit.`, body: `Offensive engineers at the NSA weaponize a flaw in Microsoft's Server Message Block (SMBv1) protocol. The exploit — ETERNALBLUE — enables remote code execution on almost any unpatched Windows machine. It is paired with a backdoor installer (DOUBLEPULSAR) and used in live operations for years. Microsoft is not told.`, artifacts: [`ETERNALBLUE (exploit)`, `DOUBLEPULSAR (implant)`, `FUZZBUNCH (framework)`] },\n        { id: 'e02', phase: 'prelude', dateLabel: 'August 13, 2016', t: 0.05, title: `The Shadow Brokers appear`, location: `Anonymous / online`, coords: [55.0,37.0], severity: 2, kind: 'leak', headline: `An anonymous group begins auctioning stolen NSA cyberweapons.`, body: `A group calling itself \"The Shadow Brokers\" posts a sample of tools stolen from the NSA-linked Equation Group and announces an auction. Researchers confirm the sample is real. Russian intelligence is widely suspected. The auction fails.`, artifacts: [`Public GitHub dump`, `Bitcoin auction wallet`] },\n        { id: 'e03', phase: 'prelude', dateLabel: 'March 14, 2017', t: 0.12, title: `Microsoft ships MS17-010`, location: `Redmond, WA`, coords: [47.6,-122.1], severity: 2, kind: 'defense', headline: `Microsoft silently patches the exact SMBv1 flaws EternalBlue exploits.`, body: `Security bulletin MS17-010 quietly closes the SMBv1 vulnerabilities. No CVE reporter is named. Observers conclude the NSA, fearing imminent leak, warned Microsoft. The patch ships — but worldwide patching cycles take months.`, artifacts: [`MS17-010 bulletin`, `Windows Update rollout`] },\n        { id: 'e04', phase: 'prelude', dateLabel: 'April 14, 2017', t: 0.19, title: `\"Lost in Translation\" dump — EternalBlue goes public`, location: `Public internet`, coords: [50.1,14.4], severity: 3, kind: 'leak', headline: `The Shadow Brokers publish the full NSA toolkit. A nation-state exploit is now globally accessible.`, body: `On Good Friday, Shadow Brokers release a trove titled \"Lost in Translation\" — EternalBlue, DoublePulsar and a chaining framework. Within 24 hours, scanners map hundreds of thousands of vulnerable hosts. The floor collapses beneath every unpatched Windows machine on the internet.`, artifacts: [`Public exploit code`, `DoublePulsar beacons in the wild`] },\n        { id: 'e05', phase: 'prelude', dateLabel: 'May 12, 2017', t: 0.26, title: `WannaCry — the warning shot`, location: `Global, DPRK-attributed`, coords: [39.0,125.7], severity: 4, kind: 'attack', headline: `A ransomware worm using EternalBlue cripples the UK NHS, Telefónica, FedEx and 200,000+ hosts.`, body: `WannaCry spreads worldwide in hours, encrypting files and demanding $300 in Bitcoin. UK NHS hospitals reroute ambulances. A researcher registers a hard-coded kill-switch domain and halts it by accident. The signal is loud — and the world ignores it.`, artifacts: [`Kill-switch domain`, `Emergency XP patch`], casualties: `~200,000 infections, 150 countries`, damageUSD: `$4–8B (est.)` },\n        { id: 'e06', phase: 'prelude', dateLabel: 'May 18, 2017', t: 0.32, title: `XData — the rehearsal`, location: `Ukraine`, coords: [50.45,30.52], severity: 2, kind: 'intrusion', headline: `Ukrainian companies hit by XData ransomware — propagating through the M.E.Doc update channel.`, body: `A first attack dubbed XData spreads in Ukraine via a compromised M.E.Doc update. In retrospect this is an access-validation phase: Sandworm has already compromised the Linkos Group update infrastructure and is testing propagation and lateral movement.`, artifacts: [`M.E.Doc update infrastructure`, `Stolen code-signing keys`] },\n        { id: 'e07', phase: 'prelude', dateLabel: 'May–June 2017', t: 0.36, title: `Covert preparation`, location: `Kyiv, Ukraine`, coords: [50.45,30.52], severity: 2, kind: 'intrusion', headline: `Sandworm (GRU Unit 74455) retains persistence inside Linkos Group.`, body: `Attackers keep covert access to the M.E.Doc update servers and weaponize a payload combining EternalBlue + EternalRomance + Mimikatz credential harvesting + lateral movement via PsExec and WMIC — a hybrid designed to pierce any segmented network. The ransom note is a deliberate misdirection.` },\n        { id: 'e08', phase: 'detonation', dateLabel: 'Jun 27 · morning · Kyiv', t: 0.42, title: `Day 0 — the poisoned M.E.Doc update`, location: `Kyiv, Ukraine`, coords: [50.45,30.52], severity: 5, kind: 'attack', headline: `A malicious update ships to every installed copy of Ukraine's dominant tax-accounting software.`, body: `One day before Ukraine's Constitution Day holiday, Sandworm pushes the update. Thousands of Ukrainian businesses, banks and government agencies install it automatically. Any company filing taxes in Ukraine runs M.E.Doc — creating systemic exposure. Infection begins within minutes.`, artifacts: [`ezvit.exe (M.E.Doc updater)`, `perfc.dat payload`, `Fake ransom note`] },\n        { id: 'e09', phase: 'detonation', dateLabel: 'Jun 27 · late morning', t: 0.46, title: `Ukrainian critical infrastructure collapses`, location: `Kyiv / nationwide`, coords: [50.45,30.52], severity: 5, kind: 'impact', headline: `Within an hour, ~10% of all computers in Ukraine are wiped.`, body: `Oschadbank, Ukrposhta, Kyiv Metro, Boryspil Airport, Chernobyl radiation monitors, Ukrenergo grid terminals, ministries — all hit simultaneously. ATMs display skulls. Chernobyl radiation sensors switch to manual. Ukraine's deputy prime minister posts a photo of a blue screen and writes \"Ta-daa!\"`, casualties: `~10% of Ukrainian computers wiped` },\n        { id: 'e10', phase: 'detonation', dateLabel: 'Jun 27 · ~noon UTC', t: 0.50, title: `Maersk: 17 minutes to global blackout`, location: `Copenhagen → worldwide`, coords: [55.68,12.57], severity: 5, kind: 'impact', headline: `The world's largest shipping company loses every domain controller — except one, by accident.`, body: `A.P. Møller-Maersk handles ~20% of global container shipping. In 17 minutes NotPetya destroys 49,000 laptops, 4,000 servers, and all 150 domain controllers. A single domain controller in Ghana survived — offline during a power outage. Its hard drive is flown to the UK under escort and becomes the seed of the entire rebuild.`, artifacts: [`The Ghana domain controller`, `Handwritten bills of lading`], casualties: `49,000 laptops + 4,000 servers destroyed`, damageUSD: `$300M` },\n        { id: 'e11', phase: 'detonation', dateLabel: 'Jun 27 · afternoon', t: 0.54, title: `Merck: vaccine production halts`, location: `Kenilworth, NJ / global`, coords: [40.68,-74.29], severity: 5, kind: 'impact', headline: `Merck loses ~40,000 devices. HPV vaccine Gardasil production stops.`, body: `Pharmaceutical giant Merck is among the first US multinationals hit. Research labs, manufacturing lines and supply-chain systems are all wiped. The CDC loans Merck doses of Gardasil from the US strategic stockpile. Merck will later win the landmark $1.4 billion insurance case.`, casualties: `~40,000 devices destroyed`, damageUSD: `$870M direct / $1.4B claim` },\n        { id: 'e12', phase: 'detonation', dateLabel: 'Jun 27 · afternoon', t: 0.57, title: `FedEx / TNT Express — Europe paralyzed`, location: `Hoofddorp, Netherlands`, coords: [52.30,4.69], severity: 4, kind: 'impact', headline: `FedEx's European subsidiary TNT is comprehensively wiped. Some systems never fully recover.`, body: `TNT Express — acquired by FedEx a year earlier — reverts to manual processes: paper labels, phone calls, physically couriering package data between hubs. Integration with FedEx's systems is set back by years. FedEx books $400M in NotPetya-related damages.`, damageUSD: `$400M` },\n        { id: 'e13', phase: 'detonation', dateLabel: 'Jun 27 · afternoon', t: 0.60, title: `Mondelēz, Saint-Gobain, Reckitt, WPP, Rosneft…`, location: `Global`, coords: [48.85,2.35], severity: 4, kind: 'impact', headline: `The worm jumps every VPN it finds. Oreo, Cadbury, Durex, Nurofen production halts.`, body: `Mondelēz loses 1,700 servers and 24,000 laptops. Saint-Gobain loses €250M. Reckitt Benckiser reports £100M of lost revenue. WPP is hit. Rosneft — a Russian state oil major — is hit too. Russia is not spared from its own weapon.`, casualties: `Dozens of multinationals`, damageUSD: `€250M · £100M · $180M` },\n        { id: 'e14', phase: 'detonation', dateLabel: 'Jun 27 · evening', t: 0.63, title: `Global incident declared`, location: `Worldwide`, coords: [51.51,-0.12], severity: 4, kind: 'defense', headline: `Major cybersecurity firms declare a global incident. Companies disconnect at scale.`, body: `Mandiant, Kaspersky, ESET and Cisco Talos issue urgent advisories. Companies begin disconnecting networks and pulling cables from critical systems. Key signals emerge: no kill switch, a single Bitcoin wallet, and this looks nothing like profit-driven crime.` },\n        { id: 'e15', phase: 'detonation', dateLabel: 'Jun 28 · Day +1', t: 0.66, title: `Supply-chain compromise confirmed`, location: `Microsoft MSRC · researchers`, coords: [44.43,26.10], severity: 3, kind: 'analysis', headline: `Microsoft and Cybereason identify M.E.Doc as Patient Zero. A \"perfc\" vaccine is published.`, body: `Microsoft publishes analysis pointing to the M.E.Doc update as the entry vector. Researchers discover a local vaccine — a read-only C:\\\\Windows\\\\perfc file — but the wave has already passed. Thousands of companies in 65+ countries are affected.` },\n        { id: 'e16', phase: 'fallout', dateLabel: 'Jun 29–30 · Days +2/+3', t: 0.70, title: `It is not ransomware — it is a wiper`, location: `Global research community`, coords: [31.78,35.21], severity: 3, kind: 'analysis', headline: `Reverse-engineering proves recovery is impossible. The ransom channel collapses.`, body: `The malware overwrites the Master Boot Record and Master File Table with no recoverable mapping. The \"installation ID\" is random bytes. The narrative pivots: this was never cybercrime — it was a destructive cyber operation dressed as one. Kaspersky names it NotPetya.` },\n        { id: 'e17', phase: 'fallout', dateLabel: 'Jul 1 · Day +4', t: 0.73, title: `Ukraine points at Russia`, location: `Kyiv`, coords: [50.45,30.52], severity: 2, kind: 'attribution', headline: `Early government signals attribute the attack to Russian actors.`, body: `The SBU and Ukrainian officials publicly accuse Russian intelligence. The CCDCOE Tallinn-based legal toolkit begins assessing its status under international law — a question that will shape insurance disputes for a decade.` },\n        { id: 'e18', phase: 'fallout', dateLabel: 'First week of July', t: 0.76, title: `Peak operational chaos`, location: `Worldwide`, coords: [40.71,-74.01], severity: 4, kind: 'impact', headline: `Global supply chains, logistics, pharma and manufacturing operate in degraded mode for weeks.`, body: `Cumulative damage estimates cross $10 billion — making NotPetya the most expensive cyber event in history. Ports accumulate backlogs. Pharmacy supply is tight. Manufacturing plants run on paper. Some firms never restore full function.`, damageUSD: `$10B+ global` },\n        { id: 'e19', phase: 'fallout', dateLabel: '~10 days after', t: 0.79, title: `Maersk rebuilds in ten days`, location: `Global Maersk operations`, coords: [51.51,-0.13], severity: 2, kind: 'recovery', headline: `10 days · 4,000 new servers · 45,000 new PCs — reinstalled from the Ghana domain controller.`, body: `Supported by Deloitte and thousands of contractors, Maersk rebuilds its entire global network in ten days using the single surviving Ghana DC as seed. Through the outage the company processes shipping manually on WhatsApp and Gmail.`, damageUSD: `$300M` },\n        { id: 'e20', phase: 'fallout', dateLabel: 'February 15, 2018', t: 0.83, title: `Five-Eyes attribution: Russian military`, location: `Washington · London · Canberra`, coords: [51.50,-0.12], severity: 3, kind: 'attribution', headline: `The White House, UK, Canada, Australia and New Zealand blame GRU Unit 74455 — \"Sandworm.\"`, body: `In a coordinated Five-Eyes statement, NotPetya is attributed to the Russian military. The White House calls it \"the most destructive and costly cyber-attack in history.\" Total damages are pegged at over $10 billion.`, damageUSD: `$10B+ (global est.)` },\n        { id: 'e21', phase: 'fallout', dateLabel: '2018 — 2020', t: 0.86, title: `Insurers invoke \"act of war\"`, location: `New Jersey · Illinois · London`, coords: [40.71,-74.01], severity: 4, kind: 'insurance', headline: `Mondelēz sues Zurich. Merck sues ACE. Century-old war exclusions are dusted off.`, body: `Mondelēz's $100M claim and Merck's $1.4B claim are denied under \"hostile or warlike action\" exclusions written for conventional warfare. Insurers argue the US attribution to the Russian military makes NotPetya an act of war. The cyber insurance industry holds its breath.` },\n        { id: 'e22', phase: 'fallout', dateLabel: 'October 19, 2020', t: 0.89, title: `DOJ indicts six GRU officers`, location: `Western District of Pennsylvania`, coords: [40.44,-79.99], severity: 3, kind: 'attribution', headline: `The US DOJ names six Sandworm officers by face and passport.`, body: `An indictment charges six GRU Unit 74455 officers by name for NotPetya, the Ukrainian power-grid attacks, Olympic Destroyer and interference in the 2017 French election. They are unlikely to ever stand trial — but their photos are now on FBI wanted posters.` },\n        { id: 'e23', phase: 'fallout', dateLabel: 'January 2022', t: 0.93, title: `Merck wins. $1.4 billion.`, location: `Union County, NJ`, coords: [40.66,-74.39], severity: 3, kind: 'insurance', headline: `A New Jersey court rules NotPetya was NOT an act of war under Merck's all-risk policy.`, body: `The court rules the \"hostile/warlike action\" exclusion cannot be stretched to cover state-sponsored cyberattacks on commercial infrastructure. Merck is entitled to the full $1.4 billion. The ruling is upheld on appeal in 2023. The precedent stands.`, damageUSD: `$1.4B awarded` },\n        { id: 'e24', phase: 'fallout', dateLabel: 'March 31, 2023', t: 0.97, title: `Lloyd's rewrites the market`, location: `Lloyd's of London`, coords: [51.51,-0.08], severity: 2, kind: 'insurance', headline: `Bulletin Y5381: all standalone cyber policies must now exclude state-backed cyberattacks.`, body: `Lloyd's requires every standalone cyber policy written by its syndicates from 31 March 2023 to carry explicit exclusions for losses from state-backed cyberattacks. The NotPetya precedent reshapes cyber insurance globally: tighter exclusions, higher premiums, reduced scope.`, damageUSD: `$10–15B cumulative` },\n      ],\n    };\n\n    const $ = (id) => document.getElementById(id);\n    const RENDERER_SRC = $('tpl-renderer').textContent;\n    const DEFAULT_PHASE_COLORS = ['#d4a03c','#dc3c28','#b4afa5','#6c9b7d','#7d8bb0','#b07d9b'];\n    const THEME_PRESETS = {\n      'cyber-dark': {\n        preset: 'cyber-dark',\n        bg: '#0d0b08', fg: '#e6dcc8', ink: '#f6f1e4', accent: '#dc3c28', panel: '#0a0907',\n        line: '#2a2620', muted: '#9a9283',\n        fontTitle: 'Fraunces', fontBody: 'Inter', fontMono: 'JetBrains Mono',\n        phaseColors: ['#d4a03c','#dc3c28','#b4afa5','#6c9b7d','#7d8bb0','#b07d9b'],\n      },\n      wavestone: {\n        preset: 'wavestone',\n        bg: '#F5F4F9', fg: '#3A3550', ink: '#16121F', accent: '#04F06A', panel: '#FFFFFF',\n        line: '#E6E4EE', muted: '#6B6580',\n        fontTitle: 'Poppins', fontBody: 'Inter', fontMono: 'IBM Plex Mono',\n        phaseColors: ['#451DC7','#228D95','#088A42','#866CDB','#3E9BA3','#D8412F'],\n      },\n    };\n\n    // Curated Google Fonts (any other family can be set via the JSON editor).\n    const FONTS = {\n      title: ['Fraunces', 'Poppins', 'Playfair Display', 'Lora', 'Merriweather', 'Cormorant Garamond', 'Spectral', 'Inter'],\n      body:  ['Inter', 'Work Sans', 'Source Sans 3', 'Roboto', 'Open Sans', 'Lato', 'Nunito Sans'],\n      mono:  ['JetBrains Mono', 'IBM Plex Mono', 'Fira Code', 'Roboto Mono', 'Source Code Pro', 'Space Mono'],\n    };\n    function fillFontSelect(el, list, value) {\n      const opts = list.slice();\n      if (value && opts.indexOf(value) === -1) opts.unshift(value);\n      el.innerHTML = '';\n      opts.forEach(name => {\n        const o = document.createElement('option');\n        o.value = name; o.textContent = name;\n        if (name === value) o.selected = true;\n        el.appendChild(o);\n      });\n    }\n\n    let config = normalizeConfig(window.CRISISMAKER_INITIAL_CONFIG || DEFAULT_CONFIG);\n\n    // ── Normalize: fill ids, derive event positions (t), derive phases ──\n    function normalizeConfig(raw) {\n      const cfg = JSON.parse(JSON.stringify(raw || {}));\n      cfg.meta = cfg.meta || {};\n      cfg.theme = Object.assign({\n        preset: 'cyber-dark', bg: '#0d0b08', fg: '#e6dcc8', ink: '#f6f1e4', accent: '#dc3c28', panel: '#0a0907',\n        line: 'rgba(230,220,200,0.12)', scale: 1,\n        fontTitle: 'Fraunces', fontBody: 'Inter', fontMono: 'JetBrains Mono',\n      }, cfg.theme || {});\n      if (!THEME_PRESETS[cfg.theme.preset]) cfg.theme.preset = 'cyber-dark';\n      cfg.layout = Object.assign({ showMap: true, showEventList: true, showSeverity: true, showArtifacts: true, showPlayback: true, mapSide: 'right' }, cfg.layout || {});\n      cfg.map = Object.assign({ mode: 'globe' }, cfg.map || {});\n      const presetPhaseColors = THEME_PRESETS[cfg.theme.preset].phaseColors;\n      let events = Array.isArray(cfg.events) ? cfg.events.slice() : [];\n      events.forEach((e, i) => { if (!e.id) e.id = 'e' + String(i + 1).padStart(2, '0'); });\n\n      // Derive position t in [0,1]\n      const allT = events.every(e => typeof e.t === 'number' && isFinite(e.t));\n      if (!allT && events.length) {\n        const times = events.map(e => { const d = e.date ? Date.parse(e.date) : NaN; return isFinite(d) ? d : null; });\n        const valid = times.filter(v => v !== null);\n        if (valid.length === events.length && Math.max.apply(null, valid) > Math.min.apply(null, valid)) {\n          const min = Math.min.apply(null, valid), max = Math.max.apply(null, valid);\n          events.forEach((e, i) => { e.t = (times[i] - min) / (max - min); });\n        } else {\n          events.forEach((e, i) => { e.t = events.length === 1 ? 0 : i / (events.length - 1); });\n        }\n      }\n      events.sort((a, b) => (a.t || 0) - (b.t || 0));\n\n      // Derive phases if absent\n      let phases = Array.isArray(cfg.phases) ? cfg.phases.slice() : [];\n      if (!phases.length) {\n        const order = [];\n        events.forEach(e => { const p = e.phase || 'all'; e.phase = p; if (!order.includes(p)) order.push(p); });\n        phases = order.map((id, i) => {\n          const members = events.filter(e => e.phase === id);\n          const start = members.length ? Math.min.apply(null, members.map(e => e.t)) : 0;\n          return { id: id, label: id, start: start, color: presetPhaseColors[i % presetPhaseColors.length] };\n        });\n        phases.forEach((p, i) => { p.end = (i < phases.length - 1) ? phases[i + 1].start : 1.0; if (i === 0) p.start = 0; });\n      } else {\n        phases.forEach((p, i) => { if (!p.color) p.color = presetPhaseColors[i % presetPhaseColors.length]; });\n      }\n      cfg.events = events;\n      cfg.phases = phases;\n      return cfg;\n    }\n\n    function generatorBackground(t) {\n      return t.preset === 'wavestone'\n        ? 'radial-gradient(circle at top right, rgba(4,240,106,0.10), transparent 24%), radial-gradient(circle at top left, rgba(69,29,199,0.10), transparent 30%), #F5F4F9'\n        : (t.bg || '#0d0b08');\n    }\n    function generatorAmbient(t) {\n      return t.preset === 'wavestone'\n        ? 'radial-gradient(circle at 20% 30%, rgba(69,29,199,0.05) 0%, transparent 50%), radial-gradient(circle at 80% 70%, rgba(4,240,106,0.04) 0%, transparent 50%)'\n        : 'radial-gradient(circle at 20% 30%, ' + hexToRgba(t.accent || '#dc3c28', 0.035) + ' 0%, transparent 50%), radial-gradient(circle at 80% 70%, rgba(212,160,60,0.025) 0%, transparent 50%)';\n    }\n\n    // ── Build a fully standalone HTML timeline from a config ──\n    function buildHTML(cfg) {\n      const t = cfg.theme || {};\n      const title = (cfg.meta && cfg.meta.title) || 'Timeline';\n      const json = JSON.stringify(cfg).replace(/</g, '\\\\u003c').replace(/-->/g, '--\\\\u003e');\n      const fontsLink = fontsLinkFor(t);\n      const sc = (typeof t.scale === 'number' && t.scale > 0) ? t.scale : 1;\n      const rootRule = sc === 1\n        ? '  #root { position: relative; z-index: 1; width: 100%; height: 100%; }'\n        : '  #root { position: relative; z-index: 1; transform-origin: 0 0; transform: scale(' + sc + '); width: calc(100% / ' + sc + '); height: calc(100% / ' + sc + '); }';\n      return [\n'<!DOCTYPE html>',\n'<html lang=\"' + ((cfg.meta && cfg.meta.lang) || 'en') + '\">',\n'<head>',\n'  <meta charset=\"UTF-8\">',\n'  <title>' + escapeHtml(title) + '</title>',\n'  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">',\n'  <link rel=\"preconnect\" href=\"https://fonts.googleapis.com\">',\n'  <link rel=\"preconnect\" href=\"https://fonts.gstatic.com\" crossorigin>',\nfontsLink,\n'  <style>',\n'    * { box-sizing: border-box; }',\n'    html, body, #root { height: 100%; margin: 0; padding: 0; background: ' + generatorBackground(t) + '; }',\n'    body { font-family: ' + cssFamily(t.fontBody || 'Inter', 'sans-serif') + '; color: ' + (t.fg || '#e6dcc8') + '; overflow: hidden; -webkit-font-smoothing: antialiased; }',\n'    body::before { content:\"\"; position:fixed; inset:0; pointer-events:none; z-index:0; background-image: ' + generatorAmbient(t) + '; }',\nrootRule,\n'    ::-webkit-scrollbar { width: 10px; height: 10px; }',\n'    ::-webkit-scrollbar-track { background: transparent; }',\n'    ::-webkit-scrollbar-thumb { background: ' + (t.preset === 'wavestone' ? '#D1CAF2' : 'rgba(230,220,200,0.12)') + '; border-radius: ' + (t.preset === 'wavestone' ? '999px' : '0') + '; }',\n'    ::-webkit-scrollbar-thumb:hover { background: ' + (t.preset === 'wavestone' ? '#A18FE3' : 'rgba(230,220,200,0.25)') + '; }',\n'    @keyframes pulse { 0%,100%{opacity:1;transform:scale(1);} 50%{opacity:0.4;transform:scale(0.85);} }',\n'    button { font-family: inherit; }',\n'  </style>',\n'</head>',\n'<body>',\n'  <div id=\"root\"></div>',\n'  <script>window.TIMELINE_CONFIG = ' + json + ';<\\/script>',\n'  <script src=\"https://unpkg.com/react@18.3.1/umd/react.development.js\" crossorigin=\"anonymous\"><\\/script>',\n'  <script src=\"https://unpkg.com/react-dom@18.3.1/umd/react-dom.development.js\" crossorigin=\"anonymous\"><\\/script>',\n'  <script src=\"https://unpkg.com/@babel/standalone@7.29.0/babel.min.js\" crossorigin=\"anonymous\"><\\/script>',\n'  <script type=\"text/babel\" data-presets=\"react\">',\nRENDERER_SRC,\n'  <\\/script>',\n'</body>',\n'</html>',\n      ].join('\\n');\n    }\n\n    // Default trio is loaded via the original variable-font URL (true italics);\n    // any custom family is appended with a standard weight range.\n    const BASE_FONTS_URL = 'https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,300..700;1,9..144,300..700&family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap';\n    const DEFAULT_FAMS = ['Fraunces', 'Inter', 'JetBrains Mono'];\n    function cssFamily(name, generic) { return '\"' + String(name).replace(/\"/g, '') + '\", ' + generic; }\n    function fontsLinkFor(t) {\n      const want = [t.fontTitle || 'Fraunces', t.fontBody || 'Inter', t.fontMono || 'JetBrains Mono'];\n      const extra = [];\n      want.forEach(f => { if (f && DEFAULT_FAMS.indexOf(f) === -1 && extra.indexOf(f) === -1) extra.push(f); });\n      let link = '  <link href=\"' + BASE_FONTS_URL + '\" rel=\"stylesheet\">';\n      if (extra.length) {\n        const q = extra.map(f => 'family=' + encodeURIComponent(f).replace(/%20/g, '+') + ':ital,wght@0,300;0,400;0,500;0,600;0,700;1,400;1,600').join('&');\n        link += '\\n  <link href=\"https://fonts.googleapis.com/css2?' + q + '&display=swap\" rel=\"stylesheet\">';\n      }\n      return link;\n    }\n\n    // ── Offline export helpers ──\n    function arrayBufferToBase64(buf) {\n      const bytes = new Uint8Array(buf);\n      let binary = '';\n      for (let i = 0; i < bytes.length; i += 8192)\n        binary += String.fromCharCode.apply(null, bytes.subarray(i, Math.min(i + 8192, bytes.length)));\n      return btoa(binary);\n    }\n    function loadScript(src) {\n      return new Promise((resolve, reject) => {\n        if (document.querySelector('script[src=\"' + src + '\"]')) { resolve(); return; }\n        const s = document.createElement('script');\n        s.src = src; s.onload = resolve; s.onerror = () => reject(new Error('Chargement échoué : ' + src));\n        document.head.appendChild(s);\n      });\n    }\n    async function fetchFontsInlined(t) {\n      const allLinks = fontsLinkFor(t);\n      const hrefs = [...allLinks.matchAll(/href=\"([^\"]+)\"/g)].map(m => m[1]);\n      let allCss = '';\n      for (const href of hrefs) {\n        let css; try { css = await fetch(href).then(r => r.text()); } catch { continue; }\n        const urlMatches = [...css.matchAll(/url\\(([^)]+)\\)/g)];\n        const reps = await Promise.all(urlMatches.map(async m => {\n          const rawUrl = m[1].replace(/['\"]/g, '');\n          try { const ab = await fetch(rawUrl).then(r => r.arrayBuffer()); return [rawUrl, 'data:font/woff2;base64,' + arrayBufferToBase64(ab)]; }\n          catch { return null; }\n        }));\n        let inlined = css;\n        reps.forEach(r => { if (r) inlined = inlined.split(r[0]).join(r[1]); });\n        allCss += inlined + '\\n';\n      }\n      return allCss ? '<style>\\n' + allCss + '</style>' : '';\n    }\n    async function buildOfflineHTML(cfg, opts) {\n      if (!window.Babel) await loadScript('https://unpkg.com/@babel/standalone@7.29.0/babel.min.js');\n      const transpiledRenderer = window.Babel.transform(RENDERER_SRC, { presets: ['react'], filename: 'renderer.jsx' }).code;\n      const [reactText, reactDomText] = await Promise.all([\n        fetch('https://unpkg.com/react@18.3.1/umd/react.production.min.js').then(r => r.text()),\n        fetch('https://unpkg.com/react-dom@18.3.1/umd/react-dom.production.min.js').then(r => r.text()),\n      ]);\n      let mapCacheScript = '';\n      if (opts.includeMap) {\n        const [landJson, countriesJson] = await Promise.all([\n          fetch('https://cdn.jsdelivr.net/npm/world-atlas@2/land-110m.json').then(r => r.text()),\n          fetch('https://cdn.jsdelivr.net/npm/world-atlas@2/countries-110m.json').then(r => r.text()),\n        ]);\n        mapCacheScript = 'window.__LAND_CACHE__=' + landJson + ';window.__COUNTRIES_CACHE__=' + countriesJson + ';';\n      }\n      let fontsBlock = '<!-- offline: system fonts only -->';\n      if (opts.includeFonts) {\n        const inlined = await fetchFontsInlined(cfg.theme || {});\n        fontsBlock = inlined || '<!-- police : l\\'intégration a échoué, polices système utilisées -->';\n      }\n      const t = cfg.theme || {};\n      const title = (cfg.meta && cfg.meta.title) || 'Timeline';\n      const json = JSON.stringify(cfg).replace(/</g, '\\\\u003c').replace(/-->/g, '--\\\\u003e');\n      const sc = (typeof t.scale === 'number' && t.scale > 0) ? t.scale : 1;\n      const rootRule = sc === 1\n        ? '  #root { position: relative; z-index: 1; width: 100%; height: 100%; }'\n        : '  #root { position: relative; z-index: 1; transform-origin: 0 0; transform: scale(' + sc + '); width: calc(100% / ' + sc + '); height: calc(100% / ' + sc + '); }';\n      return [\n        '<!DOCTYPE html>',\n        '<html lang=\"' + ((cfg.meta && cfg.meta.lang) || 'en') + '\">',\n        '<head>',\n        '  <meta charset=\"UTF-8\">',\n        '  <title>' + escapeHtml(title) + '</title>',\n        '  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">',\n        fontsBlock,\n        '  <style>',\n        '    * { box-sizing: border-box; }',\n        '    html, body, #root { height: 100%; margin: 0; padding: 0; background: ' + generatorBackground(t) + '; }',\n        '    body { font-family: ' + cssFamily(t.fontBody || 'Inter', 'sans-serif') + '; color: ' + (t.fg || '#e6dcc8') + '; overflow: hidden; -webkit-font-smoothing: antialiased; }',\n        '    body::before { content:\"\"; position:fixed; inset:0; pointer-events:none; z-index:0; background-image: ' + generatorAmbient(t) + '; }',\n        rootRule,\n        '    ::-webkit-scrollbar { width: 10px; height: 10px; }',\n        '    ::-webkit-scrollbar-track { background: transparent; }',\n        '    ::-webkit-scrollbar-thumb { background: ' + (t.preset === 'wavestone' ? '#D1CAF2' : 'rgba(230,220,200,0.12)') + '; border-radius: ' + (t.preset === 'wavestone' ? '999px' : '0') + '; }',\n        '    ::-webkit-scrollbar-thumb:hover { background: ' + (t.preset === 'wavestone' ? '#A18FE3' : 'rgba(230,220,200,0.25)') + '; }',\n        '    @keyframes pulse { 0%,100%{opacity:1;transform:scale(1);} 50%{opacity:0.4;transform:scale(0.85);} }',\n        '    button { font-family: inherit; }',\n        '  </style>',\n        '</head>',\n        '<body>',\n        '  <div id=\"root\"></div>',\n        '  <script>' + (mapCacheScript || '') + 'window.TIMELINE_CONFIG = ' + json + ';<\\/script>',\n        '  <script>' + reactText + '<\\/script>',\n        '  <script>' + reactDomText + '<\\/script>',\n        '  <script>',\n        transpiledRenderer,\n        '  <\\/script>',\n        '</body>',\n        '</html>',\n      ].join('\\n');\n    }\n\n    function hexToRgba(hex, a) {\n      if (!hex || hex[0] !== '#') return hex;\n      let h = hex.slice(1);\n      if (h.length === 3) h = h.split('').map(c => c + c).join('');\n      const n = parseInt(h, 16);\n      return 'rgba(' + ((n >> 16) & 255) + ',' + ((n >> 8) & 255) + ',' + (n & 255) + ',' + a + ')';\n    }\n    function escapeHtml(s) { return String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;'); }\n    function download(name, content, type) {\n      const blob = new Blob([content], { type: type || 'text/plain' });\n      const url = URL.createObjectURL(blob);\n      const a = document.createElement('a');\n      a.href = url; a.download = name;\n      document.body.appendChild(a); a.click(); a.remove();\n      setTimeout(() => URL.revokeObjectURL(url), 1000);\n    }\n\n    // ── Render preview + sync all controls from current config ──\n    function renderPreview() { $('preview').srcdoc = buildHTML(config); }\n\n    function syncControls() {\n      const m = config.meta, th = config.theme, ly = config.layout;\n      $('theme-preset').value = th.preset === 'wavestone' ? 'wavestone' : 'cyber-dark';\n      $('m-title').value = m.title || '';\n      $('m-sub').value = m.subtitle || '';\n      $('m-badge').value = m.badge || '';\n      $('c-bg').value = toHex(th.bg, '#0d0b08');\n      $('c-fg').value = toHex(th.fg, '#e6dcc8');\n      $('c-ink').value = toHex(th.ink, '#f6f1e4');\n      $('c-accent').value = toHex(th.accent, '#dc3c28');\n      $('c-panel').value = toHex(th.panel, '#0a0907');\n      $('c-line').value = toHex(th.line, '#2a2620');\n      fillFontSelect($('f-title'), FONTS.title, th.fontTitle || 'Fraunces');\n      fillFontSelect($('f-body'), FONTS.body, th.fontBody || 'Inter');\n      fillFontSelect($('f-mono'), FONTS.mono, th.fontMono || 'JetBrains Mono');\n      const sc = (typeof th.scale === 'number' && th.scale > 0) ? th.scale : 1;\n      $('s-scale').value = sc; $('s-scale-val').textContent = sc.toFixed(2);\n      $('l-mapside').value = ly.mapSide === 'left' ? 'left' : 'right';\n      const mp = config.map || {};\n      $('l-maptype').value = ly.showMap === false ? 'none' : (mp.mode === 'region' ? 'region' : 'globe');\n      $('l-bounds').value = Array.isArray(mp.bounds) && mp.bounds.length === 4 ? mp.bounds.join(', ') : '';\n      $('l-country').value = (mp.country != null) ? String(mp.country) : '';\n      const isReg = $('l-maptype').value === 'region';\n      $('row-bounds').style.display = isReg ? 'flex' : 'none';\n      $('row-country').style.display = isReg ? 'flex' : 'none';\n      $('t-map').checked = ly.showMap !== false;\n      $('t-list').checked = ly.showEventList !== false;\n      $('t-sev').checked = ly.showSeverity !== false;\n      $('t-art').checked = ly.showArtifacts !== false;\n      $('t-play').checked = ly.showPlayback !== false;\n      // Per-phase color pickers\n      const box = $('phase-colors'); box.innerHTML = '';\n      (config.phases || []).forEach((p, i) => {\n        const row = document.createElement('div'); row.className = 'color-row';\n        const lab = document.createElement('span'); lab.className = 'swatch-label'; lab.textContent = (p.label || p.id).slice(0, 9);\n        const inp = document.createElement('input'); inp.type = 'color'; inp.value = toHex(p.color, DEFAULT_PHASE_COLORS[i % DEFAULT_PHASE_COLORS.length]);\n        inp.addEventListener('input', () => { config.phases[i].color = inp.value; pushJson(); renderPreview(); });\n        const nm = document.createElement('label'); nm.textContent = p.label || p.id;\n        row.appendChild(lab); row.appendChild(inp); row.appendChild(nm); box.appendChild(row);\n      });\n      renderContentEditor();\n      pushJson();\n    }\n    function toHex(v, fallback) { return (typeof v === 'string' && /^#[0-9a-fA-F]{6}$/.test(v)) ? v : (typeof v === 'string' && /^#[0-9a-fA-F]{3}$/.test(v) ? '#' + v.slice(1).split('').map(c => c + c).join('') : fallback); }\n    function pushJson() {\n      $('json').value = JSON.stringify(config, null, 2); $('err').textContent = '';\n      try { parent.postMessage({ type: 'crisismaker-debrief-change', config: JSON.parse(JSON.stringify(config)) }, '*'); } catch (e) {}\n    }\n\n    // ════════════════════════════════════════════════════════════════\n    //  CONTENT EDITOR — edit every phase/event field without touching JSON.\n    //  Open/close state survives re-renders; structural edits (add / delete /\n    //  reorder) rebuild the lists, plain field edits update config in place so\n    //  focus is preserved. Reordering swaps the `t` positions of neighbours.\n    // ════════════════════════════════════════════════════════════════\n    const ceOpenEvents = new Set();\n    const ceOpenPhases = new Set();\n    const KIND_SUGGESTIONS = ['intelligence','leak','defense','attack','intrusion','impact','analysis','attribution','insurance','recovery'];\n\n    function phaseColorFor(id) {\n      const idx = (config.phases || []).findIndex(p => p.id === id);\n      if (idx >= 0 && config.phases[idx].color) return config.phases[idx].color;\n      return idx >= 0 ? DEFAULT_PHASE_COLORS[idx % DEFAULT_PHASE_COLORS.length] : (config.theme.accent || '#dc3c28');\n    }\n    const ceNum = (v) => { const n = parseFloat(v); return isFinite(n) ? n : null; };\n\n    function ceField(labelText, inputEl) {\n      const f = document.createElement('div'); f.className = 'ce-field';\n      if (labelText != null) { const l = document.createElement('label'); l.textContent = labelText; f.appendChild(l); }\n      f.appendChild(inputEl); return f;\n    }\n    function ceText(value, oninput, type) {\n      const i = document.createElement('input'); i.type = type || 'text';\n      i.value = (value == null ? '' : value); i.spellcheck = false;\n      i.addEventListener('input', () => oninput(i.value));\n      return i;\n    }\n    function ceArea(value, oninput) {\n      const t = document.createElement('textarea'); t.value = (value == null ? '' : value); t.spellcheck = false;\n      t.addEventListener('input', () => oninput(t.value));\n      return t;\n    }\n    function ceMini(label, title, onclick, disabled, danger) {\n      const b = document.createElement('button');\n      b.type = 'button'; b.className = 'ce-mini' + (danger ? ' danger' : '');\n      b.textContent = label; b.title = title; b.disabled = !!disabled;\n      b.addEventListener('click', (e) => { e.stopPropagation(); onclick(); });\n      return b;\n    }\n    function ceCard(openSet, key, chipColor, titleText, timeText) {\n      const card = document.createElement('div'); card.className = 'ce-card' + (openSet.has(key) ? ' open' : '');\n      const head = document.createElement('div'); head.className = 'ce-head';\n      const caret = document.createElement('span'); caret.className = 'ce-caret'; caret.textContent = '▶';\n      const chip = document.createElement('span'); chip.className = 'ce-chip'; if (chipColor) chip.style.background = chipColor;\n      const title = document.createElement('span'); title.className = 'ce-htitle'; title.textContent = titleText || '(sans titre)';\n      head.appendChild(caret); head.appendChild(chip); head.appendChild(title);\n      if (timeText != null) { const tm = document.createElement('span'); tm.className = 'ce-htime'; tm.textContent = timeText; head.appendChild(tm); }\n      head.addEventListener('click', () => {\n        if (card.classList.toggle('open')) openSet.add(key); else openSet.delete(key);\n      });\n      const body = document.createElement('div'); body.className = 'ce-body';\n      card.appendChild(head); card.appendChild(body);\n      return { card, head, body, chip, title };\n    }\n\n    function renderContentEditor() {\n      if (!$('phase-editor') || !$('event-editor')) return;\n      renderPhaseEditor();\n      renderEventEditor();\n    }\n\n    function renderPhaseEditor() {\n      const box = $('phase-editor'); box.innerHTML = '';\n      const phases = config.phases || [];\n      phases.forEach((p, i) => {\n        const { card, body, head, chip } = ceCard(ceOpenPhases, p.id || ('p' + i), toHex(p.color, DEFAULT_PHASE_COLORS[i % DEFAULT_PHASE_COLORS.length]), p.label || p.id, null);\n        // header controls\n        head.appendChild(ceMini('↑', 'Monter', () => movePhase(i, -1), i === 0));\n        head.appendChild(ceMini('↓', 'Descendre', () => movePhase(i, 1), i === phases.length - 1));\n        head.appendChild(ceMini('✕', 'Supprimer la phase', () => deletePhase(i), false, true));\n        // fields\n        body.appendChild(ceField('Identifiant (id)', ceText(p.id, v => { p.id = v.trim(); commit(false); })));\n        body.appendChild(ceField('Libellé', (() => {\n          const inp = ceText(p.label, v => { p.label = v; commit(false); });\n          return inp;\n        })()));\n        body.appendChild(ceField('Période (texte affiché)', ceText(p.range, v => { p.range = v; commit(false); })));\n        const row = document.createElement('div'); row.className = 'ce-row2';\n        row.appendChild(ceField('Début (0 → 1)', ceText(p.start, v => { const n = ceNum(v); if (n != null) p.start = n; commit(false); }, 'number')));\n        row.appendChild(ceField('Fin (0 → 1)', ceText(p.end, v => { const n = ceNum(v); if (n != null) p.end = n; commit(false); }, 'number')));\n        body.appendChild(row);\n        body.appendChild(ceField('Couleur', (() => {\n          const c = document.createElement('input'); c.type = 'color'; c.value = toHex(p.color, DEFAULT_PHASE_COLORS[i % DEFAULT_PHASE_COLORS.length]);\n          c.addEventListener('input', () => { p.color = c.value; chip.style.background = c.value; commit(false); });\n          return c;\n        })()));\n        box.appendChild(card);\n      });\n    }\n\n    function renderEventEditor() {\n      const box = $('event-editor'); box.innerHTML = '';\n      const events = config.events || [];\n      const phases = config.phases || [];\n      events.forEach((e, i) => {\n        const tTxt = (typeof e.t === 'number') ? e.t.toFixed(2) : '—';\n        const ref = ceCard(ceOpenEvents, e.id || ('e' + i), phaseColorFor(e.phase), e.title, tTxt);\n        const { card, body, head, chip, title } = ref;\n        head.appendChild(ceMini('↑', 'Monter', () => moveEvent(i, -1), i === 0));\n        head.appendChild(ceMini('↓', 'Descendre', () => moveEvent(i, 1), i === events.length - 1));\n        head.appendChild(ceMini('⧉', 'Dupliquer', () => duplicateEvent(i)));\n        head.appendChild(ceMini('✕', 'Supprimer', () => deleteEvent(i), false, true));\n\n        // Phase + position\n        const row1 = document.createElement('div'); row1.className = 'ce-row2';\n        const sel = document.createElement('select');\n        const ids = phases.map(p => p.id);\n        if (e.phase && ids.indexOf(e.phase) === -1) ids.unshift(e.phase);\n        ids.forEach(id => {\n          const ph = phases.find(p => p.id === id);\n          const o = document.createElement('option'); o.value = id; o.textContent = (ph && ph.label) ? ph.label : id;\n          if (id === e.phase) o.selected = true; sel.appendChild(o);\n        });\n        sel.addEventListener('change', () => { e.phase = sel.value; chip.style.background = phaseColorFor(e.phase); commit(false); });\n        row1.appendChild(ceField('Phase', sel));\n        row1.appendChild(ceField('Position t (0 → 1)', ceText(e.t, v => {\n          const n = ceNum(v); if (n != null) { e.t = Math.max(0, Math.min(1, n)); commit(false); }\n        }, 'number')));\n        body.appendChild(row1);\n\n        // Title\n        body.appendChild(ceField('Titre', ceText(e.title, v => { e.title = v; title.textContent = v || '(sans titre)'; commit(false); })));\n        // Date label + location\n        const row2 = document.createElement('div'); row2.className = 'ce-row2';\n        row2.appendChild(ceField('Date (texte)', ceText(e.dateLabel, v => { e.dateLabel = v; commit(false); })));\n        row2.appendChild(ceField('Lieu', ceText(e.location, v => { e.location = v; commit(false); })));\n        body.appendChild(row2);\n\n        // Coords + severity + kind\n        const hasCoords = Array.isArray(e.coords);\n        const latI = ceText(hasCoords ? e.coords[0] : '', () => {}, 'number');\n        const lonI = ceText(hasCoords ? e.coords[1] : '', () => {}, 'number');\n        const updCoords = () => {\n          const la = ceNum(latI.value), lo = ceNum(lonI.value);\n          if (la != null && lo != null) e.coords = [la, lo]; else delete e.coords;\n          commit(false);\n        };\n        latI.addEventListener('input', updCoords); lonI.addEventListener('input', updCoords);\n        const row3 = document.createElement('div'); row3.className = 'ce-row2';\n        row3.appendChild(ceField('Latitude', latI));\n        row3.appendChild(ceField('Longitude', lonI));\n        body.appendChild(row3);\n\n        const row4 = document.createElement('div'); row4.className = 'ce-row2';\n        row4.appendChild(ceField('Sévérité (1 → 5)', ceText(e.severity, v => { const n = ceNum(v); if (n != null) e.severity = n; else delete e.severity; commit(false); }, 'number')));\n        const kindI = ceText(e.kind, v => { const t = v.trim(); if (t) e.kind = t; else delete e.kind; commit(false); });\n        kindI.setAttribute('list', 'ce-kind-list');\n        row4.appendChild(ceField('Type (kind)', kindI));\n        body.appendChild(row4);\n\n        // Headline + body\n        body.appendChild(ceField('Accroche (headline)', ceArea(e.headline, v => { if (v) e.headline = v; else delete e.headline; commit(false); })));\n        body.appendChild(ceField('Description (body)', ceArea(e.body, v => { if (v) e.body = v; else delete e.body; commit(false); })));\n        // Artifacts\n        body.appendChild(ceField('Artefacts (un par ligne)', ceArea((e.artifacts || []).join('\\n'), v => {\n          const arr = v.split('\\n').map(s => s.trim()).filter(Boolean);\n          if (arr.length) e.artifacts = arr; else delete e.artifacts;\n          commit(false);\n        })));\n        // Casualties + damage\n        const row5 = document.createElement('div'); row5.className = 'ce-row2';\n        row5.appendChild(ceField('Pertes (casualties)', ceText(e.casualties, v => { if (v) e.casualties = v; else delete e.casualties; commit(false); })));\n        row5.appendChild(ceField('Coût (damageUSD)', ceText(e.damageUSD, v => { if (v) e.damageUSD = v; else delete e.damageUSD; commit(false); })));\n        body.appendChild(row5);\n        // Id (advanced)\n        body.appendChild(ceField('Identifiant (id)', ceText(e.id, v => { e.id = v.trim(); commit(false); })));\n\n        box.appendChild(card);\n      });\n      // shared <datalist> for kind suggestions\n      if (!$('ce-kind-list')) {\n        const dl = document.createElement('datalist'); dl.id = 'ce-kind-list';\n        KIND_SUGGESTIONS.forEach(k => { const o = document.createElement('option'); o.value = k; dl.appendChild(o); });\n        box.appendChild(dl);\n      }\n    }\n\n    // commit(rebuild): push config to JSON + preview; rebuild the editor lists\n    // only when the structure changed (add/delete/reorder).\n    function commit(rebuild) {\n      if (rebuild) renderContentEditor();\n      pushJson();\n      renderPreview();\n    }\n    function sortEvents() { (config.events || []).sort((a, b) => (a.t || 0) - (b.t || 0)); }\n\n    function moveEvent(i, dir) {\n      const a = config.events, j = i + dir;\n      if (j < 0 || j >= a.length) return;\n      const ti = a[i].t, tj = a[j].t; a[i].t = tj; a[j].t = ti;\n      sortEvents(); commit(true);\n    }\n    function deleteEvent(i) {\n      const e = config.events[i];\n      if (e && e.id) ceOpenEvents.delete(e.id);\n      config.events.splice(i, 1); commit(true);\n    }\n    function duplicateEvent(i) {\n      const src = config.events[i];\n      const copy = JSON.parse(JSON.stringify(src));\n      let n = 1; let id; do { id = (src.id || 'e') + '-' + (++n); } while (config.events.some(x => x.id === id));\n      copy.id = id;\n      const next = config.events[i + 1];\n      copy.t = next ? (src.t + next.t) / 2 : Math.min(1, (src.t || 0) + 0.02);\n      copy.title = (src.title || '') + ' (copie)';\n      config.events.splice(i + 1, 0, copy);\n      sortEvents(); ceOpenEvents.add(copy.id); commit(true);\n    }\n    function addEvent() {\n      const a = config.events;\n      const maxT = a.length ? Math.max.apply(null, a.map(e => e.t || 0)) : 0;\n      let n = a.length + 1, id; do { id = 'e' + String(n++).padStart(2, '0'); } while (a.some(x => x.id === id));\n      const e = {\n        id, phase: (config.phases[0] && config.phases[0].id) || '',\n        t: Math.min(1, maxT + 0.03), dateLabel: '', title: 'Nouvel événement',\n        location: '', severity: 1, kind: '', headline: '', body: '',\n      };\n      a.push(e); sortEvents(); ceOpenEvents.add(id); commit(true);\n    }\n    function movePhase(i, dir) {\n      const a = config.phases, j = i + dir;\n      if (j < 0 || j >= a.length) return;\n      const tmp = a[i]; a[i] = a[j]; a[j] = tmp; commit(true);\n    }\n    function deletePhase(i) {\n      const p = config.phases[i];\n      if (p && p.id) ceOpenPhases.delete(p.id);\n      config.phases.splice(i, 1); commit(true);\n    }\n    function addPhase() {\n      const a = config.phases;\n      const lastEnd = a.length ? (a[a.length - 1].end || 1) : 0;\n      let n = a.length + 1, id; do { id = 'phase' + (n++); } while (a.some(x => x.id === id));\n      const p = { id, label: 'Nouvelle phase', range: '', start: lastEnd, end: 1, color: DEFAULT_PHASE_COLORS[a.length % DEFAULT_PHASE_COLORS.length] };\n      a.push(p); ceOpenPhases.add(id); commit(true);\n    }\n\n    function applyConfig(raw) {\n      config = normalizeConfig(raw);\n      syncControls();\n      renderPreview();\n    }\n\n    function applyThemePreset(name) {\n      const preset = THEME_PRESETS[name] || THEME_PRESETS['cyber-dark'];\n      const scale = config.theme && config.theme.scale;\n      config.theme = Object.assign({}, config.theme || {}, preset);\n      delete config.theme.phaseColors;\n      if (typeof scale === 'number') config.theme.scale = scale;\n      (config.phases || []).forEach((phase, i) => {\n        phase.color = preset.phaseColors[i % preset.phaseColors.length];\n      });\n      syncControls();\n      renderPreview();\n    }\n\n    // ── Wire up controls ──\n    $('theme-preset').addEventListener('change', e => applyThemePreset(e.target.value));\n    $('m-title').addEventListener('input', e => { config.meta.title = e.target.value; pushJson(); renderPreview(); });\n    $('m-sub').addEventListener('input', e => { config.meta.subtitle = e.target.value; pushJson(); renderPreview(); });\n    $('m-badge').addEventListener('input', e => { config.meta.badge = e.target.value; pushJson(); renderPreview(); });\n    $('c-bg').addEventListener('input', e => { config.theme.bg = e.target.value; pushJson(); renderPreview(); });\n    $('c-fg').addEventListener('input', e => { config.theme.fg = e.target.value; pushJson(); renderPreview(); });\n    $('c-ink').addEventListener('input', e => { config.theme.ink = e.target.value; pushJson(); renderPreview(); });\n    $('c-accent').addEventListener('input', e => { config.theme.accent = e.target.value; pushJson(); renderPreview(); });\n    $('c-panel').addEventListener('input', e => { config.theme.panel = e.target.value; pushJson(); renderPreview(); });\n    $('c-line').addEventListener('input', e => { config.theme.line = e.target.value; pushJson(); renderPreview(); });\n    $('s-scale').addEventListener('input', e => { const v = parseFloat(e.target.value); config.theme.scale = v; $('s-scale-val').textContent = v.toFixed(2); pushJson(); renderPreview(); });\n    $('l-mapside').addEventListener('change', e => { config.layout.mapSide = e.target.value; pushJson(); renderPreview(); });\n    $('l-maptype').addEventListener('change', e => {\n      const v = e.target.value;\n      config.map = config.map || {};\n      if (v === 'none') { config.layout.showMap = false; }\n      else { config.layout.showMap = true; config.map.mode = v; }\n      $('t-map').checked = config.layout.showMap;\n      const isReg2 = (v === 'region');\n      $('row-bounds').style.display = isReg2 ? 'flex' : 'none';\n      $('row-country').style.display = isReg2 ? 'flex' : 'none';\n      pushJson(); renderPreview();\n    });\n    $('l-bounds').addEventListener('input', e => {\n      config.map = config.map || {};\n      const nums = e.target.value.split(/[,\\s]+/).map(parseFloat).filter(n => isFinite(n));\n      if (nums.length === 4) config.map.bounds = nums; else delete config.map.bounds;\n      pushJson(); renderPreview();\n    });\n    // keep the map-type selector in sync when the master toggle is used\n    $('t-map').addEventListener('change', e => {\n      $('l-maptype').value = e.target.checked ? ((config.map && config.map.mode === 'region') ? 'region' : 'globe') : 'none';\n      const isReg3 = $('l-maptype').value === 'region';\n      $('row-bounds').style.display = isReg3 ? 'flex' : 'none';\n      $('row-country').style.display = isReg3 ? 'flex' : 'none';\n    });\n    $('f-title').addEventListener('change', e => { config.theme.fontTitle = e.target.value; pushJson(); renderPreview(); });\n    $('f-body').addEventListener('change', e => { config.theme.fontBody = e.target.value; pushJson(); renderPreview(); });\n    $('f-mono').addEventListener('change', e => { config.theme.fontMono = e.target.value; pushJson(); renderPreview(); });\n    [['t-map','showMap'],['t-list','showEventList'],['t-sev','showSeverity'],['t-art','showArtifacts'],['t-play','showPlayback']].forEach(([id, key]) => {\n      $(id).addEventListener('change', e => { config.layout[key] = e.target.checked; pushJson(); renderPreview(); });\n    });\n\n    $('x-offline').addEventListener('change', e => {\n      $('offline-opts').style.display = e.target.checked ? 'block' : 'none';\n    });\n    $('l-country').addEventListener('input', e => {\n      config.map = config.map || {};\n      const v = e.target.value.trim();\n      if (v) config.map.country = v; else delete config.map.country;\n      pushJson(); renderPreview();\n    });\n\n    $('btn-add-event').addEventListener('click', addEvent);\n    $('btn-add-phase').addEventListener('click', addPhase);\n\n    $('btn-example').addEventListener('click', () => applyConfig(window.CRISISMAKER_INITIAL_CONFIG || DEFAULT_CONFIG));\n    $('btn-load').addEventListener('click', () => $('file').click());\n    $('file').addEventListener('change', e => {\n      const f = e.target.files[0]; if (!f) return;\n      const reader = new FileReader();\n      reader.onload = () => {\n        try { applyConfig(JSON.parse(reader.result)); }\n        catch (err) { $('err').textContent = 'Fichier invalide : ' + err.message; }\n      };\n      reader.readAsText(f); e.target.value = '';\n    });\n    $('btn-apply').addEventListener('click', () => {\n      try { applyConfig(JSON.parse($('json').value)); }\n      catch (err) { $('err').textContent = 'JSON invalide : ' + err.message; }\n    });\n    $('btn-json').addEventListener('click', () => {\n      const name = ((config.meta.title || 'timeline').toLowerCase().replace(/[^a-z0-9]+/g, '-') || 'timeline') + '.config.json';\n      download(name, JSON.stringify(config, null, 2), 'application/json');\n    });\n    $('btn-html').addEventListener('click', async () => {\n      const isOffline = $('x-offline').checked;\n      if (!isOffline) {\n        const name = ((config.meta.title || 'timeline').toLowerCase().replace(/[^a-z0-9]+/g, '-') || 'timeline') + '.timeline.html';\n        download(name, buildHTML(config), 'text/html');\n        return;\n      }\n      const btn = $('btn-html'), txtEl = $('btn-html-txt');\n      btn.disabled = true;\n      if (txtEl) txtEl.textContent = 'Génération…';\n      $('err').textContent = '';\n      try {\n        const opts = { includeMap: $('x-mapdata').checked, includeFonts: $('x-fonts').checked };\n        const html = await buildOfflineHTML(config, opts);\n        const name = ((config.meta.title || 'timeline').toLowerCase().replace(/[^a-z0-9]+/g, '-') || 'timeline') + '.offline.html';\n        download(name, html, 'text/html');\n      } catch (e) {\n        $('err').textContent = 'Export hors-ligne : ' + e.message;\n      } finally {\n        btn.disabled = false;\n        if (txtEl) txtEl.textContent = 'Télécharger la timeline';\n      }\n    });\n\n    // ── Boot ──\n    syncControls();\n    renderPreview();\n  })();\n  </script>\n</body>\n</html>\n";
