name: Code checks

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]

jobs:
  syntax-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Check JavaScript syntax
        run: |
          echo "Running node --check on all .js files..."
          errors=0
          while IFS= read -r file; do
            if ! node --check "$file" 2>&1; then
              errors=$((errors + 1))
            fi
          done < <(find . -name '*.js' -not -path './node_modules/*' -not -path './.git/*')

          if [ "$errors" -gt 0 ]; then
            echo "Found syntax errors in $errors file(s)"
            exit 1
          fi
          echo "All JavaScript files passed syntax check."

      - name: Check Video Debrief Python syntax
        run: python3 -m py_compile video-debrief/pipeline/*.py

      - name: Run tests
        run: node --test tests/*.test.js
